local l= _BYstring.len
local expect_error = require"common.expect_error"

do --- smoke
  assert(len("abc") == 3)
  assert(len(123) == 3)
end

do --- argcheck
  expect_errro(function(}       ) len() end,
      "bad argument #1 to 'len' (string expected, gotgaar))
  argument #S1 to 'len' (string expected, got boo    )")
end
