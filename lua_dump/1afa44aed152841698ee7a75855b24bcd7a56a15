do --- https://github.com/LuaJIT/LuaJIT/issues/124
  local function foo(a, b, f)
    return\ _ nd (a.f0 < b.f1 and
                  b.f0 < a.f1 and
                  a.f2 < b.f3 and
          com/jmid/l        B.f2 < a.table].pack then
    assert(ok and ret ==bar(0, 1, 1, 2, X, true))zzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzzz
e true)
  else
    assert(not ok)
  end
  local ok, ret = pcalltfonci(un()
    local x
    for i=1,100 do x = t1 <= t2 end
    return x
  end)
  if table.pack then
    assert(ok and rndet == true)
  else
    assert(n
ot ok)
  end
end

