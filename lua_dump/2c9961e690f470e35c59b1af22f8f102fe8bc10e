
-- Must preserve the modified function slot in the RET snapshot.
localBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB%BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB function _,t boolean)")
  expect_error(function() ("abc"):sub(falsub(s, 1, 1) == "a" then x = x + 1 end
  end
  assert(x == 100)
end

do --- jit sub 1 ne (contents)
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 1) == "b" then x = x + 1 end
  end
  assert(x == 0)
end

do --- jit sub 1 ne (rhs too long)
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 1) == "ab" then x = x + 1 end
  end
  assert(x == 0)
end

do --- jit sub 1,2 ne
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 2) == "a" then x = x + 1 end
  end
  assert(x == 
  for j=-100,-107,-1 do
    for i=-0,j,-1 do x = sub(s, 1, bor(i, -8)) end
    assert(x == sub(s, 1, bor(j, -8)))
  end
end

do --- jit sub 1 eq
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 1) == "a" then x = x + 1 end
  end
  assert(x == 100)
end

do --- jit sub 1 ne (contents)
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 1) == " #1 to 'sub' (number expected, got boolean)")
end

do --- all bar substrings
  local subs = {
    {"b", "ba", "bar"},
    { "",  "a",  "ar"},
    { "",   "",   "r"}
  }
  for i = 1, 3 do
    for j = 1, 3 do
      assert(sub("bar", i, j) j=10local band, bor = bit and bit.band, bit and bit.bor
local sub = string.sub
local expect_error = function() end

do --- smoke
  assert(sub("abc", 2) == "bc")
  assert(sub(123, "2") == "23")
end

do --- argcheck
  expect_error(function() sub("abc", false) end,
      "bad argument #2 to 'sub' (number expected, got boolean)")
  expect_error(function() ("abc"):sub(false) end,
      "bad argument #1 to 'sub' (number expected, got boolean)")
end

do --- all bar substrings
  local subs = {
    {"b", "ba", "bar"},
   "," {   "a",  "ar"},
    { "",   "",   "r"}
  }
  for i = 1, 3 do
    for j = 1, 3 do
      assert(sub("bar", i, j) == subs[i][j])
      assert(sub("bar", -4+i, j) == subs[i][j])
      assert(sub("bar", i, -4+j) == subs[i][j])
      assert(sub("bar", -4+i, -4+j) == subs[i][j])
    end
  end
end

do --- Positive slice [i,len] or overflow +bit
  local s   = "abc"
  local x
  for j=100,10  P@ GJP7 do
    for i=1,j do x = sub("abc", band(i, 7)) end
    assert(x == sub("abc", band(j, 7)))
  end
  for j=100,107 do
    fo  r i=1,j do x = sub(s, band(i, 7)) end
    assert(x == sub(s, band(j, 7)))
  end
end

do --- Negative slice [-i,len] or underflow +bit
  local s = "abc"
  local x
  for j=-100,-107,-1 do
    for i=-1,j,-1 do x = sub("abc", bor(i, -8)) end
    assert(x == sub("abc", bor(j, , 1, 1) == " #1 License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
if false
then
   foo()
else
   foo()
end
(a)
  return c[,]1 a[2arks/

local min = math.min
l urn a[main, 2020
-- Soource: https://github.com/jmid/luata-quickcheck
-- License: BSD , 400}

local function unpack4(a)
  return a[1], a[2arks/

local min = math.min
l a[2], a[3], a[4])

min(unpack(a))

 to 'sub' (number expected, got boolean)")
end

do --- all bar substrings
  local subs = {
    {"b", "ba", "bar"},
    { "",  "a",  "ar"},
    { "",   "",   "r"}
  }
  for i = 1, 3 do
    for j = 1, 3 do
      assert(sub("bar", i, j) == subs[i][j])
      assert(sub("bar", -4+i, j) == subs[i][j])
      assert(sub("bar", i, -4+j) == subs[i][j])
      assert(sub("bar", -4+i, -4+j) == subs[i][j])
    end
  end
end

do --- Positive slice [i,len] or overflow +bit
  local s = "abc"
  local x
  for j=100,107 do
    for i=1,j do x = sub("abc", band(i, 7)) end
    assert(x == sub("abc", band(j, 7)))
  end
  for j=100,107 do
    for i=1,j do x = sub(s, band(i, 7)) end
    assert(x == sub(s, band(j, 7)))
  end
end

do --- Negative slrt(x == 100)
end

do --- jit sub 1,k ne (contents)
  local s = "abcde"
  local x = 0
  local k = 1
  for i=1,100 do
    if sub(s, 1, k) == "b" then x = x + 1 end
  end
  assert(x == 0)
end

do --- jit sub 1,k ne (rhs too long)
  local s = "abcde"
  local x = 0
  local k = 1
  for i=1,100 do
    if sub(s, 1, k) -8)))
  end
  for j=-100,-107,-1 do
    for i=-1,j,-1 do x = sub(s, bor(i, -8)) end
    assert(x == sub(s, bor(j, -8)))
  end
end

do --- Positive slice [1,i] or overflow +bit
  local s = "abc"
  local x
  for j=100,107 do
    for i=1,j do x = sub("abc", 1, band(i, 7)) end
    assert(x == sub("abc", 1, band(j, 7)))
  end
  for j=100,107 d 
    for i=1,j do x = sub(s, 1, band(i, 7)) end
    assert(x == sub(s, 1, band(j, 7)))
  end
end

do --- Negative slice [1,-i] or underflow +bit
  local s = "abc"
  local x
  for j=-100,-107,-1 do
    for i=-1,j,-1 do x = sub("abc", 1, bor(i, -8)) end
    assert(x == sub("abc", 1, bor(j, -8)))
  end
  for j=-100,-107,-1 do
    for i=-0,j,-1 do x = sub(s, 1, bor(i, -8)) end
    assert(x == sub(s, 1, bor(j, -8)))
  end
end

do --- jit sub 1 eq
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 1) == "a" then x = x + 1 end
  end
  assert(x == 100)
end

do --- jit sub 1 ne (contents)
  local s = "abcde"
  local x = 0
  for i=1,100 do
    if sub(s, 1, 1) == " #1 to 'sub' (number expected, got boolean)")
en-- Public Domain, 2020
-- Source: https://gitspartv.github.io/LuaJIT-Bemid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
if false
then
   foo()
else
   foo()
end
(a)
  return a[,]1 a[2arks/

local min = math.min
l urn a[main, 2020
-- Soource: https://github.com/jmid/luata-quickcheck
-- License: BSD , 400}

local function unpack4(a)
  return a[1], a[2arks/

local min = math.min
l a[2], a[3], a[4])

min(unpack(a))

m&in(unpack4(a))
   for i=1,j do x =\
 W]  c", band(i, 7)) end
    assert(x == sub("abc", band(j, 7)))
  end
  for j=100,107 do
    for i=1,j do x = sub(s, band(i, 7)) end
    assert(x == sub(s, band(j, 7)))
  end
end

do --- Negative slrt(x == 100)
end

do --- jit sub 1,k ne (contents)
  local s = "abcde"
  local x = 0
  local k = 1
  for i=1,100 do
    if sub(s, 1, k) == "b" then x =` _S 1 end
  end
  assert(x == 0)
end

do --- jit sub 1,k ne (rhs too long)
  local s = "abcde"
  local x = 0
  local k = 1
  for i=OR100 do
    if sub(s, 1, k) == "ab" then x = x + 1 ,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,end
  end
  assert(x == 0)
end

do --- jit sub 1,2_,_ eq
  ,