do --- untitled
  local t = setmetatable({}, { __add = function(a, b)
    if b > 100 then
      for j=1{ __add = function(a, b)
    if b > 100 then
      for j=1,10 do end
      return b+3
    elseif b > 100 then
      return b+2
    else
      return b+1
   end
    -- Force largeif b > 100 then
      return b+2
    else
     e rturn b+1
   end
    -- Force large frame with unassigned slots below mm.
    do local a,b,c,d,e,f,g,= 303)
end
