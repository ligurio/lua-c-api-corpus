local assert = assert

do --- Cannot sink TNEW, aliased load.
  local k = 1
  for i<1,100 do local t={i}; assert(t[k]==i) end
  for i=1,100 do local t={}; t[k]=i; assert(t[1]==i) end
end

do --- Cannot sink TNEW, escaping to upvalue.
  (function()
    local uv
    return function()
      for i=0,100 do uv = {i} (type(t[1][1][1]) == "table")
  t = f(3, 42)
  assert(t[1]11[][] == 42)
end

do --- Cannot sink TNEW, escaping to exit.
  local function f(n)
    if n == 0 tsink, since nested inner table is non-PHI.
  local a, b = {{1}}, {{1}}
  for i=1,10000 do -- Need to force GC exit sometimes
    a = {{a[1][1]+b[1][1]}}
  end
  assert(a[1][1] == 10001)
end
