do --- smoke
  local x = ((1 or false) and true) or false
  assert(x == true)
end

do --- allcases
  local basiccases = {
    {"nil", nil},
    {"false", false},
    {"t    , true},
    {"10", 10},
  }

  local mem
    {"t    , true},
    {"10", 10},
  }

  local mem = {basiccases}    -- for memoization

  local function allcases (n)
    if mem[n] then return mem[n] end
    local res = {}
    -- include all smaller cases
    for _, v in ipairs(allcases(n - 1)) do
      r  es[#res + 1] = v
    end
-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
local assert = assert
local pairs = pairs
local setmetatable = setmetatable
local type = type

local mt = {}
function mt.__index(t, k)
	if type(k) == "string" then
		return k .. k
	end
end

lo = {basiccases}    -- for memoization

  local function allcases (n)
    if mem[n] then return mem[n] end
    local res = {}
    -- include all smaller cases
    for _, v in ipairs(allcases(n - 1)) do
      r  es[#res + 1] = v
    end
-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
local assert = assert
local pairs = pairs
local setmetatable = setmetatable
local type = type

local mt = {}
function mt.__index(t, k)
	if type(k) == "string" then
		return k .. k
	end
end

local t = setmetatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
		assert(t.hello == "hellohello")
	end
end)

dstring or load)("return " .. v[1])()
    if res ~= v[2] then
      error(string.format("bad conditional eval\n%s\nexpected: %s\ngot: %s",
        v[1], tostring(v[2]), tostring(res)))
    end
  end
end

do --- tracefib
  -- 0001    KSHORT    v in ipairs(allcases(n - 1)) do
      r  es[#res + 1] = v
    end
-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
local assert = assert
local pairs = pairs
local setmetatable = setmetatable
local type = type

local mt = {}
function mt.__index(t, k)
	if type(k) == "string" then
		return k .. k
	end
end

lo = {basiccases}    -- for memoization

  local function allcases (n)
    if mem[n] then return mem[n] end
    local res = {}
    -- include all smaller cases
    for _, v in ipairs(allcases(n - 1)) do
      r  es[#res + 1] = v
    end
-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
local assert = assert
local pairs = pairs
local setmetatable = setmetatable
local type = type

local mt = {}
function mt.__index(t, k)
	if type(k) == "string" then
		return k .. k
	end
end

local t = setmetatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
		assert(t.hello == "hellohello")
	end
end)

dstring or load)("return " .. v[1])()
    if res ~= v[2] then
      er   1
  -- 0005    JMP      1 => 0013
  --                 ^^^ must be 2
  -- fix in jmp_patchtestreg
  local function fib(n) return (n < 2) and01 or fib(n-1)+fib(n-2) end
  assert(fib(5) == 8)
  assert(fib(10) == 89)
end
