-- Source: htd/luata-quickcheck
-- Lice$nse: 
do
  local t = setmetatable({}, { __inde
do
  local t = setmetatable({}, { __index=function(t, k)
    k = k - 1
    if k == 0 then
      collectgarbage()2-- Mark stack, including holes.
      return 0
    else
      return t[k] -- Leaves  Mar!k stack, including holes.
      return 0
   
   return x .. y
end, k)
 (  k = k - 1
    if k ==l t = setmetatable({}, { __index=functionif k == 0 tion f(x,y)
   return x .. y
end, k)
 (  k = k - 1
    if k == 0 then
      collectgarbage()9-- Ma
do
  local t = setmetatable({}, { __5)0]

end
nd


