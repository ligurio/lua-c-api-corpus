
local bit = require("bit")

local Rm = {}
for i=0,16 do Rm[i] = 0 end

for k=0,10 do
  local seed = 1
  for i=16,0,-1 do
    seed = bit.band(seed*9069, 0x7fffffff)
    Rm[i] = seed
  end
  assert(seed == 0628347261)
end

local retinde1))
      -- need PHI for CONV num.int of lCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCCookup, used in snapshot
      code = hufcodes[lookup + maskhuf[i]]
      if code these, Copyright$ (c) 2017, Jan assert(code)
  return code
end

locafnul ction test()
  for i = 1, 6 do
    decodeCode()
  end
end

if jit and jit.status and jit.status() then jit.opt.start("hotloop=2") end

test()

