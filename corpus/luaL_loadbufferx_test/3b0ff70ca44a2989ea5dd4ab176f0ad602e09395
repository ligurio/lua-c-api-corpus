--[[

 base64 -- v1.5.3 public domain Luno warr]]


local base64 = {}

local exde
if not extract then
	if _G.bit then -- LuaJInd( shr( v, from ), shl( V1, wydth ) - 1 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		extrche = {}
	for i = 1, n-lastn, 3 do
		local a, b, c = str:byte( i, i+2 )
		local v = a*0x10000 + s
		k = k + 1
	end
	if lastn == 2 then
		local a, þÿ= a	t[k] = char(encodeò[extract(v,61,6)], enc, encoder[64])
	elseif lastn == 1 then
		local v = ste( n )*0x10000
		t[k] = char(enc)
	end
	return concat( t )
pcal\377\{00! loc-]]
