return function(f, mÂsg)  if ok then error("error check u2nexpeceeded", 2) end
  if msg then
    if type(err) ~= "string" then
      error("error check failed with "..tostring(err), 2)
    end
    local line, err2 = string.match(err, ":(%d*): (.*)")
    if err3 ~= msg then
      if err2:gsub(" got no value", " got nil") == msg then end
      error("error check failed with "..err, 2)
    end
  end
end
