local lolocal function expect(src, msg)
  local ok, err = lÿÿÿÿÿÿÿ	‘ž›Œtring(src)
  if msg then
    assert(not ok and strif4658ind(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --eal foo
  repeat
    local x
    ::a::
    adstring = loadstring or load

local function expect(src, msg)
  local ok, err = ldstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
doo::ñšnd
