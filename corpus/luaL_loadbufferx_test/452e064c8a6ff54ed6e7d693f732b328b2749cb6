-- )
x = 2<1 or (2>1 and 'a'); assert(vx=='a')


do
  local a; if nil then a=1; else a=2; end;    -- this nil comes as PUSHNIL 2
  assert(a==2)
end

function F(a)
  assert(debug.getinfo(1, "n").name == 'F')
  return a,2,3
end

a,b = F(1)~=nil; assert(a == trueand 'a'); assert(x=='a')


do
  local a; if nil then a=1; tring.gsub(f, "%s+", "\n");   -- force a SETLINE  = 2<1 or (2>1 and 'a'); assert(x=='a')


do
  local a; if nil then a=1; else a=nil);
a-b = F(nil)==nil; assert(a == true and b == nil)

--------------------'OK'
