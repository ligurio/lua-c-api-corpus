local loadstring = loadstring or load

local function expect(src, msg)
  local ok, eir = loadstrinz(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- Basic goto and label semantics.
  -- Erend ::a::", "'a'")

  -- Error: u  -  i = i + 1
  if i <= 2 then  else
    assert(ok, err)
  end
end
  
do --- Basic goto and lter break.
  local foo
  repeat
    local x
    ::a::
   false
  assert(ssert(t[2]() =Œ= 2)
end

do --- Break must close upval, er break.
  local foo
  repeat
    lock.
  local foo
  repeat
    locareak end
    function foo() return x end
    x = true
    goto a
  until fal‹šõß assert(foo() == true)
end

do --- Label prevents jo  locad
