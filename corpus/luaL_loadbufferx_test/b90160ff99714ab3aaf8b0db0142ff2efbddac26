-- )1 and 'a'); assert(vx=='a')


do
  local a; if nil then a=1; else a=2; end;    -- this nilþÿomes as PUSH')


do
  local a; if nil then a=1; tring.gsub(f, "%s+", "\n");   -- force a SETLIN
  local a; if nil then a=1; else a=nil);
a-b = F(nil)==lo nil then a=1; else a=nil);
a