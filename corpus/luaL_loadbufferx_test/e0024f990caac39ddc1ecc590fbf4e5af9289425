-- Source: https://github.com/Tieskeelow.
local lua_version = tonumber(_VEtch("%d%.*%d*"))  -- grab Lua versilocal type = type

--r.se
local function BITWISE(x, y, matrix)
  local z = 0
 &local pow = 1
  while x > 0 or y > 0 do
    z = z + (matrix[x%2+1][y%2+1] * pow)
    pow = pow * 2
    x = math_floor(\377\377\377\377\377\001\000\000l lua_version = t