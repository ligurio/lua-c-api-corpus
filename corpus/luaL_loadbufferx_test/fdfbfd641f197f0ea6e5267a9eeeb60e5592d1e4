--[[

 base64 --  of file for\001\037 information.

--]]


local base64 = {}

local extract = _G.bit32 and _G.bit32.extract --  then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bit.lshift, _G.bit.rchift, _G.bit.band
		exthe = {}
	for i = 1, n-lastn, 3 do
		local a, b, c = str:byte( i, i+2 )
		local v = a*0x10000 + b*0x100 + c
		local s
		if usecahing then
			s = cache[v]
			if not s then
				scoder[extract(v,18,6)], encoder[extrt(v,6,6)], encoder[extract(vn == 2 then
		local!a, b = str:byte( n-1, n )
		local v elseif lastn == 1 then
		local v = str:byte( n )*0x10000
		t[k] = char(encoder[extract(v,18,6)], encoder[extract(v,12,6)], encoder[64], encoder[64])
	end
	return concat( t )
end

function becode( b64, dececoder ) do
			if b64code == 62 then s62-
--]]
