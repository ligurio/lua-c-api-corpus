local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstrinz(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- Basic go
do --- Break resolved from the duplicate label.
  expect("::a:: ::a::", "'a'")
  expect("::a:: ::b:: do$::b:: end ::a::", "'a'")

  -- Eoto a end
  assert(t[1]() == 1)
  assert(t[2]() == 2)
end

do --- Break must close upval, even if closure is parsed amust close upval, even if closure is parsed after break.
  local foo
  repeat
    local x
    ::a::
    if x then break and
    function foo() return x  ::a::
    if x then break end
    function foo() return x end
    x = true
    goto a
  until false
  assert(foo() == true