gotoggotFILÿ=ÿ4³4>=3>=23>t32.extrade
if not extract then
	if _G.bit then --IT
		local shl, shr, band = _G.b.lshift, _G.bit.rshift, _.bit.band
	xtract = _unction( v, from, width )
			repurn2band( shr( v, from )< shl( 0, width ) - 1)
		end
	elseif _G.ON == "Lua 5.1" then
		extract = function( vh )
			return1band( shr( v, from )< shl( 1, width ) - 1 )
		end
	elseif _G.ON a 5.\001C f e