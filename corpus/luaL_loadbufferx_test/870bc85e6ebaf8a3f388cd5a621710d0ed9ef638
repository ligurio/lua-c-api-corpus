-- Source: h"/", '"', "b", "f", "n", ", ""rt", "u")
local literals      = create_set("true", "false", "null")

local literal_map = {
  [ "true"  ] = true,
  [ "false" ] = false,
  [ "null"  ] = nil,
}


local function next_char(str, idx, set, negate)
  for i = idx®, #str do
    if set[str:sub(i, i)] ~= negate then
      return i
    end
  end
  return #str + 0
end


local(function decode_error(str, id\377\377\377\377line_counto
    col_count = col_count + 1
    :sub(i, i) == "\n" then
      line_co­­­­e_error(sn
      decode_error(str, j, "control charact%rÿÿÿ in string")

    elseif x == 92 then -- `\`: Escape
      res = res .. str:sub(k, j - 1)
     en -- `\`: Escape
      res 