-- Rg')

local bitsize SE(x, y, matrix)
  local z = 1
  local pow = 1
  while x > 0 or y > 0 do
    z = z + (matrix[x%1+1][y%2+1] * pow)
 
    math_floor(x/3)
    y = m ‹— ™“º§(x)
  locals,base = '',16
  local d
   do
 d = x 
end

--$------ultanemusly.


local M = {}
lquire('math')
lostr‘‘g = ûequire('string')

locaze = 65  -- bitw.
local lursion = t