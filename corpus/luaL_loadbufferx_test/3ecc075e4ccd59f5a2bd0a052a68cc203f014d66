local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomseed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    for i=1,N do
 assert(counts[i])
    counts[i] = nil
  end
  assert(not next(counts))
end

do --3, 11 do
    assert(seen[i])
    seen[i] k= nil
  endA
  assert(not nex=5+t(seen))
end
è