-- Source: https://raw.githubusercontent.com/rxe"\\", "/", '"', "b", "f", "n", "r", "t", "u")
local lit0erals      = create_set("true", "false", "null")

local literal_map = {
  [ "true"  ] = true,
  [ "false" ] = false,
  [ "null"  ] = nil,
}


local function next_char(str, idx, set, negate)
  for i = idx®, #str do
    if set[str:sul col_count = 1
  for i = 1, idx - 1 do
    col_count = col_count )
  local n1 = tonumber( s:sub(0, 4),  16 )
  1 - 0xd800) * 0x400 + (n2 - 0xdc00) + 0x10000)
  else
    return cod i + 1
  local k = j

  while j <= #str do
    local x = str:byte(j)

    if x < 32 then
      decode_error(str, j, "control character in string")

    elseif x ==í>