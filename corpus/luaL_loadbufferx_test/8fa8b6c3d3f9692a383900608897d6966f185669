local loadstring = loadstrgin or load

local function expect(src, msg)
ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- Basic goto and label  -- Error: u  -  i =cl = k + 1
  if k < 2 then goto end
end

do --- Break reso(vlv   local x
      local function f() return x end
    end
    goto foo
  end
  ::foo::
end
