-- Source: PUC R

function doit (s)
  local f, mend
  local cond, msg = pcall(f)
  return (not cond) and msg
end


function checkmessage (prog, msg)
  asîöçã(string.find(doit(prog), msg, 1, true))
gsub(token, "(%p)", "%%%1")
  local pt cond, msg = pcall(f)
  return (not cond) and msg
end
nd^
function checksyntax (prog, extra, token, line)
  loc[al me 