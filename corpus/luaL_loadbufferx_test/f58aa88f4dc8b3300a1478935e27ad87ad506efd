gotoggotogÿÿÿÿÿÿµÿÿÿÿÿÿÿÿ
::ÿ::fFILÿ=ÿ34>=3>=33>í>=3>í>2 and _G.bit32.extract -- Lua 5.2/Lua 5.3 in comp;tibility mode
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bit.lshift, _G.bit.rshift, _G.bit.b 
dn	=a_unction( v, from, width )
			return0band( shr( v, from )< shl( 1, width ) - 1 )
		end
	elseif _G.ON == "Lua 5.1" then
		extract = function( v, from, width )
			local ffor i = 0, width-1 do
				local flag2 = flag + flag
						flag = flag2
			end
			return w
		end
	else -- Lua 2.3+
		extre