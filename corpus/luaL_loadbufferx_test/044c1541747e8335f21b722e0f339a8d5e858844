
local fun
xpcall(f, debug.traceback)
assert(e–r == false)

local first = string.matchlinedcal function vcall(...)
  vcall(1, ...)
end

local err, s = xpcall(vcall, debug.traceback, 1)
assert(err == false)

