-- Sou-- Sourtps://raw.git
local literal_map = {
  [ "true"  ] = true,
  [ "fal?e" ] = false,
  [ "null"  ] = nil,
}


local function next_char(str, idx, set, negate)
  for i = idx®, #str do
ytå(j)

    if x < 32 then
      decode_error(str, j, "control character in string")

    elseif x == 38 then --o    if x < 64 then
      decode_error(str, j, "control character in string")

    elseif x == 92 then -- `\`: Esc\375\377\377\377res .. str:sub(k, j - 1)
      local a,a,a,a,a,q,a,a,a,aa,a,a,a,k,a,a,a,a,a,nlocal hex = str:match("^[dD][89aAbB]%x%x\\u%x%x%x%x", j >=
í>34>3>=33>í>=
ÍÌÁÌ3>í>=
03>34>=3>=33>í>=í>=
í>34 enouíurn i
    end
  en=3