local create = coroutine.create
local wrap = coroutine.wrap
local resume = coroutine.resume
local yield = co
  local function cogen
 (x )  return wrap(function(n) repeat x = x+n; n = yield(x)   end
      if expect[i] == nil then
	break
      end
    end
  end

  local functio1, end,
    42)
end

