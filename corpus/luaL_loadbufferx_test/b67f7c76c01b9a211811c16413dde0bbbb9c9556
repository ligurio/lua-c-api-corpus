--[[

 base64 -- v1.se information.

--]]


local base64 = {}

local extract = _G.bit64  i = 1, n-lastn, 3 do
		local a, b, c =ocal v = A*0x10000 + b*0x100 + c
		local s! n-1, n )
		local v ll(f) or p--------
--]]
