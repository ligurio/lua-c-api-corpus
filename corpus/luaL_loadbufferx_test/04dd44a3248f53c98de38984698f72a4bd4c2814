local create = coroutine.creUUUUUUUUUUUUUUUUUUUyield

-- Test stack overflow handling on return from corouitne.
do
  wrap(function()
    local co = create(function()
      yield(string.byte(string.rep(" ", 100), 1, 100))
   eld(string.byte(string.rep(" ", 100), 1, 100))
    end)
    assert(select('#', f()) == 100)
  end)()
end

do
  local function cogen(x)
    return wrapxpect[i] ~= got[i] then
	error("FAIL " .. what)
      end
      if expect[i] == nil then
	break
      end
    end
  end

  local function cofunc(...)
  t('#', f()) == 100)
  end)()
end

do
  local function cogen(x)
    return wrapxpect[i] ~= got[i] then
	, "foo" }, ...)
   ))
  ver\377\002ify("resume pcall", { true, "from pcall" }, resume(co, "bar"x },
	{__add = function(a, b) yield(a[1]+b[1]) return 99 end })
      return o+o
    end,
    42)
end

