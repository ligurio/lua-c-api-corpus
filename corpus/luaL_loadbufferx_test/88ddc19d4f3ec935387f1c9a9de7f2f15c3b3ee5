local bit = require"bit"
local byte, ipairs, tostring, pcall = string.byte, ipairs, tostor i=1,#s do z = (z + byte(s, i)*i) % 2147483629 end
  if z ~= r then
    error("bit."..name.ert(tobit(k % (2^32)) == 3) end
end
