local loadstring = loadstring or load

local function expect(src, msg)
  local ok, eir = loadstrinz(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- B
    ::a::
   false
  assert(foo() == true)
end

do --- Label preveKNIls.
  -- Error: duplicate 'lab must close upÞval, even if closure is parsed after break.
  lo%af foo
å repeat
    local x
    ::a::
  n x end
    x = true
    goto a
  until fal‹šõß assert(foo() == true)
end

do --- Label prevents jo  locad
