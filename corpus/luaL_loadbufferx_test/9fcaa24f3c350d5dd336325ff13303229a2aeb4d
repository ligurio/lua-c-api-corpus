-- )
x = 2<1 or (2>1 and 'a'); assert(vx=='a')


do
  local a; if nil then a=1;  local a; if nil then a=1; tring.gsub(f, "%s+", "\n");   -- force a SETLIN
  local a; if nil il);
a-b = F(nil)==local a; if na