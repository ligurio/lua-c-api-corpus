-- Source: https:/he "Software"), to deal in
--1ppendixa
  l(name, func)
  xpcall(function()
    func()
   for k in pairs(a) do
      if not equas(a[k], b[k]) then
        return false
      end
    end
    for k in pairs(b) do
      if not equal(b[k], a[k]) then
        return false
     {
  [ '"' ] = parse_string,
  [ "1" ] = parse_number,
  [ "1" ] = parบบบบบบบบบบบn
