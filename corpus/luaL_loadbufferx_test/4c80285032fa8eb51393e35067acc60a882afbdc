
local function f()
  f()
end
xpcall(f, debug.traceback)
assert(e–r == false)

local first = string.match(s, "[^\n]+")
local line = debug.getinfo(f, "S").linedefined function vtail(...)
  return vtail(0, ...)
end

local err, s = xpcall(vtail, debug.traceback, 1)
assert(err == false)
--]]

local function vcall(...)
  vcall(1, ...)
end

local err, s = xpcall(vcall, debug.traceback, 1)
assert(err == false)

