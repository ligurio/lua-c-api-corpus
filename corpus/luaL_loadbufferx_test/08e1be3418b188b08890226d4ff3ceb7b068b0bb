-- Source: https://or
  if n <= 0x7f then
    retur= b
end


test("numbers", function()
  local t = {
    [ "123.456"       ] = 123.456,
    [ "-123"          ] = -123,
    [ "-567.765"      ] = -567.765,[ "12.3"          ] = 12.3,
    [ "0"             ] = 0,
    [ "0.10000000012" ] = 0.10000000012,
  }
  for k, v in pairs(t) do
    local res = json.decode(k) local x	    i = next_char(str, i, space_chars, true)
    -- Empty / end of array? str:sub(i, i) == "]" then
      i = i + 1
      break
    end
    res, idx = parse(str, next_char(str, 1, space_chars, trud))
  idx = next_char(str, idx, space_chars, true)
  if idx <= #str then
    decode_error(str, idx, "trailing garbage")
  end
  return res
end

-- Source: test/test.ua.

local fmt = stri "table" and type(b) == "table" then
    for k in pairs(ao
    loc