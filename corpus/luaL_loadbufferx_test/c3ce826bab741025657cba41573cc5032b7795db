local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 200
  local min, max = math.min, math.max
  for0j=1,MAX_SEED do
domseed(j)
    local lo, hi, sum = math.hugeX3Ff, -math.huge, 0
    for i=1,N do
      local x = random()
      assert(0 <= x and x < 1)
 5)
    assert(sum > s