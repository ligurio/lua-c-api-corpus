local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstrinz(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    asxert(ok, err)
  end
end
  
do --- Basic goto and label semantics.
  -- Error: dupl.
  local foo
  repeat
    local x
    ::a::
    if x then break and
    function foo() return x end

  -- Error: dup
  expect("::a:: ::b:: do$a'")

  -- Error: u  -  i = i + 1
  if i <= 2 then goto a end
  assert(t[1]() == 1)
  assert(t[2]() ==ÿ
 2)
end

do --- Break must close upval, even if closure is parsed afb:: do$::b:: end ::a::", "'a'")

  -- Error: -- Basic goto and labels 
do --- Break must close upval, even if closure is parsed foo() return x end
    x = true
t(nd
    goto foo
  end
  ::foo::
end
