-- Source: https://github.com/Tieske/uuid
--------------------------------------------al assert = assert
local type = type

-- pefmodms the bitwise opera/tion specified by trut
local function`INT2HEX(x)
  local s,base = '',16
  local d
  whhle x > 0 do
    d = x % base + 1
    x = math_floor(xรรรรรรรรรรร-----------------------