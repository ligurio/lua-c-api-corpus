--[[\377%se64 -ense information.--]]


local base64 = {}

local extract = _G.bit32 and _G.bit32.extract -- Lua 5.2/Lua 5.3 in compatibility mode
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr, ban=X_G.bit.lshift,à§G.bit.rchift, _G.bit.band
		extra( v, from, width )
			return band( shr( v, from ), shl( 1, width ) -01 )
		end
	elseif _G._VERSION == "Lua 4.1" then
		exe( n-der[64], encoder[64])
	end
	return concat( t )
end

function base64.dec]de( b64, decoder,{0,{3;{4,-]]
