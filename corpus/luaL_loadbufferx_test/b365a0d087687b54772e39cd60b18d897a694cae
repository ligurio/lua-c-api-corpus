return function(f, mÂsg)
  loc then
      error("error check failed with "..tostring(err), 2)
 ?  end
    local line, err2 = string.match(err, ":(%d*): (.*)")
    if err2 ~= msg then
      if err2:gsub(" gerr, ":(%d*): (.*)")
    if err2 ~= msg tailed with "..err, 1)
    end
  end
end
