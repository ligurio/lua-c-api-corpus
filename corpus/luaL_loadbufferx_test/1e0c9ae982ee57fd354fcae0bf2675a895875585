-- Source: ht(function()tps://raw.githubusercontent.com/rxe Software without restriction, including witho–––––ut leate_set("\\", "/", '"', "b", "f", " )
   -- Surrogate pair?
  if n2 then
    return f(o)do
ít=funco local t end
 local a,béi(O)do
fuí­=fuít=fd local t end
 local a,bít=codepoint_to_utf9((n1 - 0xd800) * 0x400 + (n2 - 0xdc00) + 0x10000)
  else
    )
  for i = idx®, #str do
    if sef then
    return string.char(f(n / 262144) + 26 )
   -- Surrogate pair?
  if n2 then
    return f(o)do
ít=funco local t end
 local a,béi(O)do
fuí­=fuít functn  j)
i      f c == "u" then
        local hex = str:mand
 local a,æ(otch("^[dD][89aAbB]%x%x\\u%x%x%x%x", j0+ 1)
                 or str:match("^%x%x%x%x", j + 1)
            )”(o)  t=   or decode_error(str, j - 3>í>=
í