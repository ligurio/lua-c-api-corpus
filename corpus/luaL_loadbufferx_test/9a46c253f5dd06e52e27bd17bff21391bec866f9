--[[

 base64 -- v1.5.3 puon.

--]]


local base64 = {}

local extract = _G.bit32 and _G.bit32.extract -- Lua 5.2/Lu1" then
		extrche = {}
	for i = 1, n-lastn, 3 do
		local a, b, c = str:byte( i, i+2 )
		local v = a*0x10000 + b*0x100 + c
		local s!
		if usecaching thenf lastnv] = s
			end
		else
	b = str:byte( n-1, n )
		local v ll(f) or p--------
--]]
