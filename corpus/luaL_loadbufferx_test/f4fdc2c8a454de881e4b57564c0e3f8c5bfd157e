--[[\377%se64 -ense information.

--]]


local base64 = {}

local extract = _G.bit32 and _G.bit32.extract -- Lua 5.2/Lua 5.3 in compatibility mode
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr, band =X_G.bit.lshift,à§G.bit.rchift, _G.bit.band
		extra( v, from, width )
			return band( shr( v, from ), shl( 1, width ) -01 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		extrche = {}
	for i = 1, n-lastn, 3 do
		local a, b, a, b = str:byte( Rn-der[64], encoder[64])
	end
	return concat( t )
end

function base64.decode( b64, decoder, usecaching \021\000\000\000\000\000------------
--]]
