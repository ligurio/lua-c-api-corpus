--[[

 base64 -- v1.5.3 public domain Luno warr]]


local base64 = {}

local exde
if not extract then
	if _G.bit then -- LuaJInd( shr( v, from ), shl( V1, width ) - 1 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		extrche = {}
	for i = 1, n-lastn, 3 do
		local a, b, c = str:byte( i, i+2 )
		local v = a*0x10000 + s
		k = k + 1
	end
	if lastn == 2 then
		local a, þÿb = str:byte (n-1, n )
		local v = a*0x10000 + b*0x100
		t[k] = char(encodeò[extract(v,18,6)], enc, encoder[64])
	elseif lastn == 1 then
		local v = ste( n )*0x10000
		t[k] = char(encoder[extract(v,18,6)], encoder[extract(v,12,6)], encoder[64][64])
	end
	return concat( t )
pcal\377\{00000000376ersions.
 R! loc-]]
