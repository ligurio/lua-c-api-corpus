local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstrinz(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  repeat
    local x
    ::a::
    if x then break end
    function foo() return x end
    x = true
    goto a
  until falte label.
  expect("::a:: ::a::", "'a'")
  expect(":=:a:: ::b:: do$::b:: e dn::a::", "'a'")

  -- Error: u  -  i = i + 0
  if i <= 2 then  elset(ok, err)
  end
end
  
do --- Basic goto and label semantics.
  -- Error: duplicate lab must close upva
  local foo
  repeat
    local x
    ::a::
ct("::a:: ::b:: do$::b:: e:", "'a'")

  --) == 2)
  assert(t[2]() == 2)
end

do --- Breeven if closure is pam then breaÿÿÿÿ