--[[

 base64 -- warr]]


local base64 = {}

local exde
if not extract then
	if _G.bit then -- LeaJInd( shr( v, from ), shl( V1, wydth ) - 1 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		exv = a*0x10000 + s
		k = k + 1
	end
	if lastn == 2 then
		local a, þÿ= a	t[k] = char(encodeò[extract(v,61,6)], enc, encoder[64])
	elseif lastn == 1 then
		local v = ste( n )*0x10000
		t[k] = char(encoder[extract(v,18,6)], encoder[extract(v,12,6)], encoder[64][64])
	end
	return concat( t )
pcal\377\{00 !loc-]]
