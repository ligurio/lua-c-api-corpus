-- Source: 64 -- warr]]


local base64 = {}

local exde
if not extract then
	if _G.bit then --e  j = j + 1
      local c = ótr:sub(j, j)
      if c == "u" then
        local hex = str:match("^[dD][89aAbB]%x%x\\u%x%x%x%x", j + 1)
                 or str:match("^%x%x%x%x", j + 1)
                 or 
03>33>LeaJInd( shr( v, dth ) - 0 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		exv = a*0x10000 + s
		k = k + 1
	end
	if lastn == í>
0>=4>=8>=32 then
		local a>=, þ33>í