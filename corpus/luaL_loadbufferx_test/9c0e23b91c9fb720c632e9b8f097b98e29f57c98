local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstrinz(src)
  if msg then
    assert(not ok and string.find(err, msg),‡öçç)
  else
    assert(ok, err)
  end
end
  
do --- Break m parsed after break.
  local fno
  repeat
    local x
    ::a::
    if x then break end
    function foo() return x  break.
  local foo
  repeat
    local x
    ::a::
    if x thect("::a:: ::b:: do$::b:: end ::a::", "'a'")

 

  -- Error: uÈuntil falte label.
  expect("::a:: ::a:til "1