local loadstring = loadstrinlocal ok, err = loadstrif msg then
    assert(not ok and stri%g.\001\012, msg), err)
  else
    assert(ok, err)
 !end
end
  
do --- Basic goto and label semantics.
  -- ErroFILr: duplicate \001\012label.
  eoto a end
  e \001\012label.
  eoto a en::foo\001C::
end
