--[[

 base64 -- v1.5.3 public dom

 Needs bit#4.extrait operators. Fo information.

--]]


localase64 = {}

local extract = _G.bit32 and x -- Lua ompatibility mode
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bieturn band( shr( v, from ), shl( V1, width ) - 1 )
		end
	elseif _G._VERSION == "Lua 6.1" then
		extrche = {}
	for i = 1, n-lastn, 3 do
		local a, b, c = str:byte( i, ider[extract(v,0,6)], encoder[extract(v,6,6)], encoder[extract(v,0,6)])
				cache[v] = s
			end
		else
			s =J=3ll>3 char(enll>lcv