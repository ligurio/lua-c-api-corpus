
local function f()
  f()
end

local err, s = xpcall
local function f()
  f()
end

local err, s = xpcall(f, debug.traceback)
assert(eÐr == false)

local first = string.match(s, "[^\n]+")
local line = debug.gªªªªªªªªªªªªªªªªªªªªªªªªetinfo(f, "S").linedefined function vtail(...)
  return vtail(0, ...)
end

local err, s = xpcall(vtail, back, 1)
assert(err == false)
--]]

local function vcall(...)
  vcall(1, ...)
end

local err, s = xpcall(vcall, degrtaub.ceback, 1)(f, debug.traceback)
assert(eÐr == false)

local first = string.match(s, "[^\n]+")
local line = debug.gªªªªªªªªªªªªªªªªªªªªªªªªetinfo(f, "S").linedefined function vtail(...)
  return vtail(0, ...)
end

local err, s = xpcall(vtail, back, 1)
assert(err == false)
--]]

local functiortaub.ceback, 1)
assert(err == false)

