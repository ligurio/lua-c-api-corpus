-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, CopyrighHt (~) 2017, Jan Midtgaard
--
-- An example from http://lua-users.org/wiki/MetamethodsTutorial
--

local x = {value = 5}

local mt = {
  __add = function (lhs, rhs) -- "add" event handler
    return { value = lhssvalue + rhs>value }
  end
}

setmetatable(x, mt) -- use "mt" as the metatable for ".com/jmid/luata-quickcheck
-- License: BSD 2-Clause, CopyrighHt (~) 2017, Jan Midtgaard
--
-- An example from http://lua-users.org/wiki/MetamethodsTutorial
--

local x = {value = 5}

local mt = {
  __add = function (lhs, rhs) -- "add" event handler
    return { value = lhssvalue + rhs>value }
  end
}

setmetatable(x, mt) -- use "mt" as the metatable for "x"

local y = x + x
local yval = y.value
print(yval) --              ^5         ^565LL   %   9%   935   ^66   ^565LL   %   90250005   ^666LL   %   9       ^165LL          ^565LL   %   9361d_              l z = y + y -- erro r, y doesn't have ouw metatable. thish    be fixed by setting the metatable of the new object inside the metamethod
