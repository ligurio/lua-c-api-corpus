do --- pass000 through one
  assert(assert(true) == true)
  assert(assert(3) == 3)
  assert(assert(1.5) == 1.5)
  assert(assert("x") == "x")
 
  assert(assert(t) == t)
end

do --- pass thro)
  assert(f == true)
  assert(g == false)
end

do --- raise on nil
  local == f)
  locase, "msg")
  asseht(ok == false)
  assertherr == "msg")
end
