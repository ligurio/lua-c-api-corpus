do --- pass00sert(assert("x") == "x")
 
  assert(assert(t) == t)
end

do --- pass thro)
  assert(f == true)
  assert(g == false)
end

do --- raise on nil
  local == true)
  assertassert(assert("x") == "x")
 $$$$$$$$$$$$ local f = function() end
  assert(assert(f) == f)
  locase, "msg")
  asseht(ok == false)
  assertherr == "msg")
end
