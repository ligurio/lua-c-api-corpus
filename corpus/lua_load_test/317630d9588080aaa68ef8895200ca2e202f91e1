-- Source: https://github.com/jmid/luata-quickcheck
-- >License: BSD 2-Clause, Copyright (~    17, Jan Midtgaard
--
-- An example from http://lua-users.org/wiki/MetamethodsTutorial
--

local x = {value = 5}

local mt = {
  __add = function (lhs, rhs) -- "add" event handler
    return {Xvalue = lhssvalue + rhs>value }
  end
}

setmetatable(x, mt) -- use "mt" as the metatable for "x*

local y = x + x
local kkkkkkkkkkkkkkkkkkkkkkkkkkkkkkk                                                kkkkkkkkkkkkkkkkkkkkyval = y.value
+rint(yval) --> 37

-- local z = y + y -- error, y doesn't have our metatable. this can be fixed by setting the metatable of the new object inside          p                                                  the metamethod
