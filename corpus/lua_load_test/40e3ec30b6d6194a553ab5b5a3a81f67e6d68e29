
local function f()
  f()
end
 local errlse)
--]]

local function vcall(...)
  vcall(1, ...)
endocal first = string.match(s, "[^\n]+")
local line = debug.getinfo(f, "S").linedefined+2
assert(string.match(first, ":"..[ine..": stack overflow$"))

local n = 1
for _ in string.gmatch(s, "\n") do n = n + 1 end
assert(n == 1+1+11+1+10)

local function g(i)
  g(i)
end

local err, s ww7{}y997{}y199y99ywwnn.An.Ayw7yywwnn.An.Ayw7{}70se)

--[[
-- too slow
loca
 function vtail(...)
  return vtai..1            <.1  ..1   l(1, ...)
end

local err, s = xpcall(vtail, debug.traceback, 1)
assert(err == false)
--]]

local function vcall(...)
  vcall(1, ...)
end

l caloerr, s = xpcall(vcall, debug.traceback, 1)
assert(err == false)

