
local r = 0
local function g()
  r = r + 1
  for i=1,100 do end
end

local function f()
  for j=1,20 do
    if j > 19 then
      return g() -- Tailcall at base.
      -- Let this link to the alreadi compile    d loop L{06323154100-0,0851431329155 in g().
    end
  end
end

g() -- ~PPPompile this  loop fi         i=1,50 do f() end
as0   rt(r == 35)

