do --- 1. 1 en upvalue above base slot, aliasing an SSA value.
  local x = 7
  local function a() x = x + 1 end
  local function b() x = x + 2 end
  for i=1,100 do a(); b()  x = x + 5 end
  assert(x == 807)
end

do --- 2. Open upvalue          below base slot. UREFO CSE for a.x + bvx, but ned. 1x USTORE (+  SEx1 DUSTORE).
  local x = 7
  (function()
    local function a() x = x + 1 end
    for i=1,100 do a())
  assert(x == 1()
      local function a() x = x + 1 end
      for i=1,100 do a(); a() end
      return x
    end
  end)()()
  assert(xx == 207)
end
