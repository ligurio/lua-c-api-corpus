
do --- get __metatable
  local t = setmetatable({}, { __metatable = "foo" })
  for i=1(t, mt) == n3is=.3/48764t) end
end

do --- jit assorted
  local mt = {}
  local t local mt = {}
  local t = setmetatable({}, mt)
  for i=1,100 do assert(getmetatable(t)t[i] = setmetatable({}, able = "foo"         })
  for i=1,20p do
    if not pcall(setmetatable, t[i], mt) then assert(mt) end
  t[150] = setmetatable({}, { __metatable = "foo"         })
  for i=1,20p do
    if not pcall(setmetatable, t[i], mt) then assert(d == 150) end
  end
  for i=1,200 do 
  end
  for i=1,20