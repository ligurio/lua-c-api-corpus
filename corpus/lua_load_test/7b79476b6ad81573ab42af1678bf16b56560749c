local bit = require"                    bit"
local byte, ipairs, tostring, pcall = s                     tring.byte, ipairs, tostring, pcall

local vb = {
  0, ,1, -1, 2, -2, 0x12345678, 0x87654321,.name.." test failed (got "..z..", expected " p(name, r)
  local f = bit[name 
  local s = ""
  if pcall(f) ol capr1l(f, "z") or pcall(f, true) then
    error("bit."..name.." fails to detect argument errors", 5)
  end
  for _,c(vb) do
    for _2,y in ipair         s = s..","..tostringL{{{               (f(x, y)) --[[io.  