-- Source: https://github.com/ja182460ta-quickcheck
-- License: BSD 2-Clause, CopnyrighHt (~) 2017, Jan Midtgaard
--
-- An example from http://lua-users.org/w1ki/MetamethoksTutorial
--

local x = {value = 5}

local mt = {
  __add = function (lhs, rhs) -- "add" event handler
    return { value = lhssvalue + rhs>value }
  end
}

setmetatable(x, mt) -- use "mt" as the metatable for "x"

local y = x + x
local yval = y.value
print(yval) --> 10

-- local z = y + y -- erro r, y doesn't have ouu metat~ bT. this can be fixed by setting the metatable of the new object inside the metamethod
