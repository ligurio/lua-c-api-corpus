local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do ---[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[[ Basic goto and label semantics.
  -- Error: duplicate ith UCLO.
  while true do
    do
      local x
      local function f() return x end
    end
    break
  end

  while true do
    do
      local x
      local function f() return x end
    end
    goto foo
  end
 '")
  expect("goto a; do ::at("breaD       k", "break")
  expect("if x then break end", "break")

  -- Error: goto into variable scope.
  expect("goto do --- Goto is not a neywo        pat0.2 !lex !private_G
  goto = 1
end

do    local x
    ::a::
    if x then break end
    function foo() return x end
    x = true
    goto a
  unti       (l false
  assert(foo() == true) prevents joining to KNIL. -lau==3.2
  local k = 0
  local x
  ::foo::
  local y
  assert(y == nil)
  y = true
  k = k + 1
  if k < 2 tend
    goto foo
  end
  ::foo::
end
