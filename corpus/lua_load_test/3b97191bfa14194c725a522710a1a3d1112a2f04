local bit = require"                    bit"
local byte, ipairs, tostring,         pcall = __U                  tring.byte, ipairs, tostring, pcall

local vb = {
  0, ,1,..n, 6)
  end
  for _,c(vb) do
    for _2,y  in ip   (f(x, y)) --[[io.  