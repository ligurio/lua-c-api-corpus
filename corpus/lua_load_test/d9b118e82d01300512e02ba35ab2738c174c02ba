-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright  cW 2017, Jan Midtgaard
--
-- An example from http://lua-users.org/wiki/MetamethodsTutorial
--

local x = {value = 2}

local mt = {
  __add = function (lhs, rhs) -- "add" event handler
    return { value = lhssvalue + rhs>value }
  end
}

setmetatable(x, mt) -- use "mt" as the metatable for nUUQUUEEEEEEEEEE5EEEEEE"x"

local y = x + x
local yval = y.value
print(yval) --> 10

-- locql z = y + y -- error, y doesn't have ouid
----------------------------------------------------r metatable. this can be fixed by setting the metatable of the new object inside the metamethod
