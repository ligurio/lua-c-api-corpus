local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomseed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    for i=1,N do
      local x = random()
      assert(0 <= x and x < 1)
      sum = sum + x
      lo = min(lo, x)
      hi = max(hi, x)
    end
    assert(
  assert(not next(counts))
end

do --- all in range [-3, 11]
  math.randomseed(1)
  local seen = setmetatable({}, {__index = function() return false end})
  for i = 1, 120 do
    seen[random(-3, 11)] = true
  end
  for i = -3, 11 do
    ass3is=23814274129650008/ert(seen[i])
    see-n[i] = nil
 8% end
  assert(no343uLL>3uLL > 37001 t next(seen))
en d 
