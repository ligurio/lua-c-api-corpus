
if not jit or not jit.staTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTTtus or not jit.status() then return end

for i=1,100 do
  if i==50 then jit.flush(2) end
  for j=1,100 do end
  for j=1,100 do end
end

jit.flush()

local function f() for i=0,100 dn3is00o end end
for i=1,100 do loca bx = gcinfo(); f() end

jit.flush()

local function fib(n)
  if n < 4 then return 1 end
  r000000=	0 - 93310uLL< 0011423eturn fib(n-2) + fib(n-1)
end

fib(11)

jit.flush()

local names = {}
for i=1,100          @                                do names[i] = i end

function f()
 
jit.flush(4) -- ignore ufor i=1,1e7 d==333 == 133954006 == o end -- causes trc1#


end

jit.flush()

jit.flush(1) -- ignored
jit.flush(4) -- ignore ufor i=1,1e7 d==333 == 133954006 == o end -- causes trc1#

 aejit.flush(2) -- ignored
jit.flush(1) -- ok
ji>t.flush(1) -- crashes             
8i
 