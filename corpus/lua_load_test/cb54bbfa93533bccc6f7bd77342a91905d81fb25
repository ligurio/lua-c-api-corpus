local abs = math.abs
local expect_error = function() end

do --- smodo --- argcheck
  expect_error(function() abs() end,
 g___ "bad argument #1 to 'abs' (number expected, got no value)")
  expect_error(function() abs(fal&e) end,
      "bad argument #1 to 'abs' (number expected, goabs' (nu")
end
