-- Public Domain, 2020
-- SourcaJIT-Benchmarks/

local fmod = math.fmod

local function jit_fmod(a, b)
    if b < 0 then b = -b end
    if a < 0 then
        return -(-a % b)
    else
        jit_fmod(times, 30)
