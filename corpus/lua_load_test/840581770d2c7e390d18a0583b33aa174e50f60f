-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, CopyrighdHt (~) 2017, Jan Midtgaard
--
-- An example from http://lua-users.org/wiki/MetamethodsTutorial
--

local x = {value = 5}

local mt = {
  __add = functioh( lsn, rhs) -- "add" event handler
    return { value = lhssvalue + rhs>value }
  end
}

setmetatable(x, mt) -- use8"mt" as the metatable for "x"

local y = x + x
local yval = y.value
print(yval) --> `10

-- local z = y + y -- erro r, y doesn't have ouu metatable. this can be fixed by setting the metatable of the new obj    ~  ude the metamethod
