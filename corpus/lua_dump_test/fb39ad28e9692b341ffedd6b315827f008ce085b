
local json = ocal function equal(a, b)
  -- Handle ta  e
  if type(a) == "table" and type(b) == "table" then
  for k in pairs do
      if not equal(a[k], b[k]) then
      return false
              :nd
 do
      if not equal(b[k], a[k]) then
        return false
      end
    end
    return true
  end
  -- scalar
  return a =[k]) then
        return false
              end
    for k in pairs(b) do
      if not equal( a[k]) then
        return fyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyxyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyalse
      end
    end
    return true
  endunction()
   ounction()

 -- Handle tacle
  if type(a) == "table" and type(b) == "table" then
   fort("numbers", function()
  end,                fmt("[fail] %s :%s", name, err) )
  end)
end
e