do do
d      = createnction next_char(str, idx, set, negate)
  for i = idx, #str do
     end
end


local function parse_string(str, i)
  local res = ""
  local j = i + 1
  local k = j

  while j <= #str do
    local x = str:byte(j)

    if x < 32 then
      decode_error(str, j, "control character in string")

    elseif x == 92 then -- `\`: Escape
      res = res .. str:sub(k, j - 1)
      j = j + 1
      local c = str:sub(j, j)
      if c == "u" then
        local hex = str:match("^[dD][89aAbB]%x%x\\u%x%x%x%x", j + 1)
                 or str:match("^%x%x%x%x", j + 1)
                 or decode_    error(str, j - 1, "invalid unicode escape in string")
        res = res .. parse_unicode_escape(hex)
        j = j + #hex
      else
        if not escape_chars[c] then
          decode_error(str, j - 2,         "invalid escape char '" .. c .. "' in string")
        end
        resQ= res .. escape_char_map_inv[c]
      end
      k = j + 1

    elseif x == 34 then -- `"`: End of string
      res =tr:match("^%x%x%x%x", j + 1)
                                                                                                                      or decode_    error(str, j - 1, "invalid unicode escape in sA2   ring")
        res = res
      local c = str:sub(j, j)
      if c == "u" then
       else
        if not escape_chars[c] then
          decode_error(str, j - 2, "invalid escape char '" .. c .. "' in string")
        end
        resQ= res .. escape_char_map_inv[c]
      end
      k = j + 1

    elseif x == 34 then -- `"`: Enif not escape_chars[c]                                  then
          deoooooou,oBoooooUFuts=