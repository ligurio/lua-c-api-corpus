local bit = require"bit"
local byd
goto        { x^goto{  not no                    { x^goto {
               { x^goto{ _binop(name, r)
  loca! f = bit[name]
  local s = ""
  if pcb) do
   ll(f, true) or pcall(f, 1, tror("bit."..n 2^32)) == 3) end
end
