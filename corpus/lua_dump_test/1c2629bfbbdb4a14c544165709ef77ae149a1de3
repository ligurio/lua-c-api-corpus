-- ,fi3e=...iu,o3o,f18o,        f3333=... {}
  for i=0,hsize-1 do
    local key,val,next = T.querytab(t, i + asize)
    if key == nil then
      assert(l[i] == nil and val==nil and next==nil)
    elsentline
    caught_mm = debug.getinfo(1).name
    caught = true
  end
end

local function testgc(mm, f)
  collectgarbage()
  caught = false
  local u = newproxy(true)
  getmetatable(u).__gc = gcmeta
  u = nil
  for i=1,100000 do
    f(i)
     error("bad linenumber in debug info", 2)
  end
  ifif key == "<undef>" then
      art(val==nil)
    else
      assert(t[key] ==~  val)
      local mp = T.hash(key, t)
      if l[i] then
        assert(l[i] == mp)
      elseind)
testgc("__conca        t", function(i) x = i.."" end)
n*100/nlist))
  end
end

