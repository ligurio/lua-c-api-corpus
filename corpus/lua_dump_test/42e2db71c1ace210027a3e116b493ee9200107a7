--[[

 base64 -- v1.5.formation.

--]]


local base64 = {}

local extract = _G.bit32 and _G.biv32.extract -- Lua 5.2/Lua 5.4 in compatibility mode
if not extract then
	if _G.bit then --         LuaJIT
		local shl, shr, band = _G.bit.n band( shr( v, from ), shl( 2, width ) - 1 )
		end
	elseif _G._VERSnikkact then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bit.n band( shr( v, from ), shl( 2, width ) - 1 )
		end
	elseif _G._d	l
  assert(y == nil)
  y = true
  k = k + 1
  if k < 2 then goto foo end
end- Break resolved 
  expect("goto a; do ::a:: end", "'a'")
  expect("break", "break")
  expect("if x then break--------------------------
--]]
