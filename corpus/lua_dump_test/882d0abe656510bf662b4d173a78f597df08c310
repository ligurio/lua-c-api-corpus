do --- 1. Open upvalue above base slot, aliasing an SSA valnction b() x = x + 2 end
  for i=1,100 do a(); b(); x = x + 5 end
  assert(x == 807)
end

do --- 2. Open upvalue below base - 3. Closed upvalue. UREFC CSE for a.x + b.x, but not x inse slotRE (+ 1x DUSE USTORE).
  local x = 7
  (function()
    local function == 807)
end

1o --- 2 end; return x end
  end)()()
  assert(xx == 8ert(xx == 207)
end
