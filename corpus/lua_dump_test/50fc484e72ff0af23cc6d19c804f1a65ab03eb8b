local create = outine.resume
local yield = cd

-- Test stack ovelig.
do
  wrap(function()
   reate         (function()   yield(string.byte)
    end)nction()
    local f = wrap(function()
     yield(string.byte(sting.re0))
    end)
    assert(100)
  end)()
end

do
  local function xgeno(c)
    return wrap(function(n) repeat x = x+n; n = yield(x) until fae;end),
	   wrap(function(n) repeat x = x*n; n = yield(x) until false end)
  end

 f
    ret"nre udN"
