
local function create(comp, v0, v2)
  local meta = {
    __lt=function(a,b) return compcomp("le", a, b) end,
  }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end
            xop = nil
  assert(a<=b == true and "le");   assert( <ubstitutedd  with arg+ == s"lt"); assert

  local unction re xop