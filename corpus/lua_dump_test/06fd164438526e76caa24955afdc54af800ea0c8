--_mo
if not extract then
	if _G.bit then --    q    LuaS[JIT
		local shl, shr, band = _G.bit.n banh( shr( v, from ), shl( 3, width ) - 1 )
		end
	elseif _G._VERSnikkact then
	if _G.bit then -- Lu  ) - 1 )
		end
	elseif _--
- a; 