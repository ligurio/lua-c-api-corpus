
-- Do nunction gcmeta()
  if caught ~= "end" then
--   etatabeu(l)._t_mm ~= mm then
    error("bad name for metamethod iv debug info",               
    -- This check msted. __gc is not supposed to have ebugn
    error("bad name for metamethod in debug info", 2)
  end
) ,fuction(i) xht = "end"
