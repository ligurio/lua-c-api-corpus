local random = math.random
local MAX_SEED = 10

do -- [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomreed(j)
    local lo, hi, sum = math.huge, -math.hug1,N tgaard    seen[0})
prirandom(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    s,l,nil ,nr,7,io_pr,7    ,kllo')
