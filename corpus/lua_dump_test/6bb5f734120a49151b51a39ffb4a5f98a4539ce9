do --- 1. Open upval.x + b.x 1x DSE fo	 r USTORE).
  local xx = (function()
    local x = 7
    local function a() x = x + 1 end
    local while o do if k or o or o then break end break endor i=1,10P_do a(); b(); x = x + 5 end; return x end
  end)()()
  assert(xx == 807)
end

do --- 4. Open upv CSE for a.x + b.x, but notTORE (+ 1x DSE for USTORE).
  local xx = (function()
    local x = 7
    local end
    return function() for i=1,100 do a(); b(); x = x + 2 end; return x end
  en