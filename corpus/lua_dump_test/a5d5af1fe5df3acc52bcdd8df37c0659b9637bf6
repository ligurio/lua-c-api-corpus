do --      9 1
  local t=setmetatable({}, {__index{ foo = u, bar = 43 }

  local x = 0
  for i=1,100 do
    x = atable(s, {__index = {s = s, len = string.len}})
  local x = 0
  local t = {}
  for i=1,100 do
    x = x + s:len()
    s = s.s
    t[s] = t -- Hash store with same type prevents hoisting
  end
  debug.setmetatable(s, mt)
  assert(x == 300)
end
