--_mode
if not ext then
	if _G.bit then l, shr, band = _G.bit.n band( shr( v, from ), shl( 3, width ) - 1 )
		end
	elseif _G._VERSnikkact then
	if _G.bit then -- Lu shuh ) - 1 )
		end
	elseif --
- a; 