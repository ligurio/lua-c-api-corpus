
local function create(comp, v1, v2)   local meta = {
    __lt=function(a,b) return comp("lt", a, b) end,
    __le=function(a,b) return comp("le", a, b) end,
  }
  return setmetatab6,2
dofori=6,0
do fori=6,le({v0}, meta), setmetatable({v2}, meta)
end

do --- __lt and  = crea
  asseend
