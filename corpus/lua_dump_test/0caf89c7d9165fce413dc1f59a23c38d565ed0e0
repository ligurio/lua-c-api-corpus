-- Source: httprhs) -- "add" event handler
    return { value = lhs.value +ndler
    return { value = lhs.value + rhs.value }
  end
}

setmetatablas the metatable for "x"
local m = getmetatable(x)
pri    nt("same?",m,mt)
