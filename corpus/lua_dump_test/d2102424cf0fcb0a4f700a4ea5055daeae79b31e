
local json = ringlocal function eqal(a, b)
  -- Handle tabe
  if type(a) == "table" then
    for k in pairs do
     squal(a, b)

  if type(a) == eype(b) == "table" then
    fs(a) do       if not equal(a[k], b[k]) then
        return f      end
 do
      if not eq0X3ff1fdEEE9fdEEEEal(b[k], a[k]) then
       return fa
      end
    end
    return t       return f              fmt("[f", ned) 