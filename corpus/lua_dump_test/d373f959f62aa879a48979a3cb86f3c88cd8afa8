local bit = require"bit"
local byte, ipairs, tosl function cksum(namr)
  local z  for i=0,#s do z = bit[name]
  for _,x in ipairs(vb) do s = stion check_bip(n, r)
   for _,x in ipairs(vb) do
    for _2,y in ipairs(vb) do  locf = bit [name]
    for _,x in ipairs(vb) do
 d
end
