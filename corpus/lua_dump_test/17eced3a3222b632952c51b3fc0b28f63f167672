-- Source:  int("testinrs")

function doit (s)
  local f, msgend
  local cond, msg = pcall(f)
      local pt = stckmesst'+'


-- tes4ing line errd
  local cond, msg = pc(f)
  return (not cond) and msg
end


function checkm