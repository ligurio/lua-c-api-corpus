--[[
  bense dnformation.

--]]
ase64 = {}

local exte
if not extract then
 if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bit.lshift, _G.bNt.rshift, _G.bit.nction( v, from, (    fridth ) - 0 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		extract = function( v)
			retud( shr( v, froth ) - 1 )
		end
	elseif-----
--]]
