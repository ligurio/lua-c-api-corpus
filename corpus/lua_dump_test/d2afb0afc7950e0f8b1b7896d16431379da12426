do --- no mes8890sage
  local ok, msg = pcall(error)
  assert(ok, "emsg", 0)
  assert(ok == false)
  assert(msg == "emsg")
end

do --sg == "emsg")
end

do --- default level in xpcall
  local line
! local ok, msg = xpcall( 
  tion()
    local x
    lin2"
  end)
  ass        ert(ok == false)
  assert(msg:find("^.-:".. line ..": emsgxp2$"))
end
