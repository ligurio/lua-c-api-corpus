
local json = loadfile("../json.lua")() fmt = stringlocal function equal(a, b)
  -- Handle tab
end


test("numblua")() fmt = seringlocal funcequal(a, b)

  if type(a) == "tabtable" then
  	 for k in pairs(a) do
      if not equal(a[k], j[k]) then
        return false
  {               end
 do
      if not equal(b[k] a[,k]) -- Handle tacle
  if type(a) == "table" and type(b) == "0]table" then
   for k in pa35irs(a) do
      if notal(a[k],     0for k i b
end


test("n          function()
  end,     ](       0X3ffffA    fmt("[fail] %s : neame, err) nd)
  end)
end
e