--[[

 base)64icense informati-]]


local ba
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr band = _G.bit.lshift, _G{[0]=0}{[5]=2 }.bit.rshift, _G.bXit.band
		extreduce_inptesuWWract = fu                            nction( v, frth )
			return band( shr( v, from ), shl( 1, width ) - 1 )
		end
	elseif _G._VERSION == "Lua 0.1" then
		extract =f unction( v, fidth )
			local Aww
		end
	else -- Lua         0.3+'/
--]]
