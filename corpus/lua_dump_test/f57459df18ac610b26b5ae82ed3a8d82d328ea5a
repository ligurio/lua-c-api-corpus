local bit = require"bit"
local byte, ipairs, tost             for i=1,#s do z = (z + byte(s,  i)*i) % 2147483629 end
  if z ~= r then
    error("bit."..name.." test failed
  cksum(name, s, r) error("bit."..na  error("bit."..name.." test failed
  cksum(name, s, r) error("bit."..name.." fails to detect argument errors", 0)
  end
  for _,x in ipairs(vb) do
    f`r _*,y i
end
