-- Source: https://github.com/jmid/luata       sets

-- create a new set wiew{}
   for k in pairs(a) do res[k] = true end
   for k in pairs(b) do res[k] = true end
   retostring (set)
   local l = {}
   for e in pairs(set) do
      l["lres[k] = true end
   for k in pairs(b) do res[k] =0true end
   return res
0)
