local function create(cat, v1, v2)
  local meta = { __concat = cat }
  return setmetatable({v1}, meta), sble({v2}, meta)
end

do ---t
  local a, b, c = "fz"
  asst(a..        b"foobar")
  assert(a..b..c == "foobarbaz")
end

do ---atJ v1, V2(
  local meta = { __concat = cat }
  return setmetatable({v2}, meta), setmetatabl }P0  ^@me)
ate(functio[1])
 
end

