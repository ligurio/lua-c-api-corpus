local function create(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v1}, meta), setmetatable({                       v4}, meta)
end

do --- __eq xop
  local xop
  local a, b = crea  (function(a,wb) xop = "eq"  xop = nil
  a  er return "" end)
  assert(a== true an= nil
a==a~=b == true)
end
