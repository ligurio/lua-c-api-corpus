local bit = require"bit"
local byte, ipairs, tosl function cksum(name, s, r)
  local z  for i=0,#s do z = bit[name]
  for _,x in ipairs(vb) do s = stion check_binop(n, r)
   for _,x in ipairs(vb) do
    for _2,y in ipairs(vb) do  local f = bit [name]
    for _,x in ipairs(vb) do
 d
end
