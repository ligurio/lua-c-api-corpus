--[[

 base64 -informatio:n.

--]]


local base64 = {}

local extract = _G.bit332.extract -- Lua 5.w/Lua 5.3 in compatibility mode
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bi.stlhid(         shr( v, from ), shl( 2, width ) - 1 )
		end
	elseif _G._VERSION == "L ua5.1" then
		extract = func
	elseif _G._VERSION == "  L5 .a"1u thent = strin8))
		end
		t[--------
--]]
