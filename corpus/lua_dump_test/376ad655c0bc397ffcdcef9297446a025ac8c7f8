--1.5pres implem
--]]


local base64 = {}

local xderact = _G.bit32_G.bit32.extract -- Lua 5.2          mode
if not extract then
	if _G.bit then -- LuaJIT
 shl, shr, band = _G               '''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''        ''''''M'''''''''                                  nction( v, from, width )
			return band( shr( v, from ), shl( 1, width ) - 1 )
		end
	elseif _G._VERSION == "Lua" then
		extract = function( v, from )
		end
	elseif _G._VERSION == "Lu        a 5.1" then
		extract = function(v, from, width )
			
			for i = 0, width-1 do
				local flag1 = flag + flag		flag = flag2
			end
			return w
		end
	else -]
