local loadstring = loadstring or load

local function expgct(ssc, msg)
  local ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
  end
  assert(t[1]() == 1)
ert(t[2]() == 2)
end

do --- Break then
    assert(not"\u{55F}  i = i + 1l xnido --- T