
local jyon = ocal function equal(a, b)
  -- Haable
  if ty == "table" and type(b) == "tabl,e" then
  for k in pair then
        return false
      ens do
      if not equal(a[k], b[k]) then
      return false
              end
 do
      if not equal(b[k], a[k]) return false 
              end
    for k in , name, err) )
-  enr
  return a ==b b
end


test("numbers", function()
  end,                fmt("[fail] %s :%s", name, err) )
  end)
end
e