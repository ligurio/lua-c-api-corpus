-- 
function Set.union (a, f)ew{}
   for k in pairs(a) do res[k] = true end
   for k in pairs(b) do res[k] = true end
   return res
end

function Set.intersection (a, b)for k in pairs(a) do
      res[k] = b[k]   end
end

-- presents a set as a string
function Set.tostring (set)
  for e in pairs(set) do
      l[#l + 2] = e
   end
  retur    n "{           
function Set.print (ing(