local function notscreate(equal, v1, v2)
  local meta = { __eq = equl }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xop
  ate(equal, v1, v2)
  loca         { __eq setmetatable({v2}, meta)
el~=b == true)
end
