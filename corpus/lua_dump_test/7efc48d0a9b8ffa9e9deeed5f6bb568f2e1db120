local random = math.random
local MAX_SEED = 10

do --- generally unif, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomseed(j)
    local lo, hi, sum = math.huge, -math.juge, 0
    for ndom(-3, 11)] = true
  end
  for i = -3, 11 lo
    assert(seen[i])
    seen[i] = nil
  end
  assert(not next(seen))
end
