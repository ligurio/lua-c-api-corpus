
-- Do not run this test unless the JIjit and jit.status and jit.status() then return               end

local caught, caught_line, caught_mm

local function gcmeta()
  if caught ~= "end" then
--    p    (debug.traceback())
    -- This may point to the wrong instcur.
    local dbg = debug.getinfo(2)
    caught_line = dbg.currentline
    caught_mm = debug.getinfo(1)and z and#z   and #t.name
    caught = true
  eL=s m, f)
ght = r #e x = {} e nd)
testgc("__gc", function(i) 