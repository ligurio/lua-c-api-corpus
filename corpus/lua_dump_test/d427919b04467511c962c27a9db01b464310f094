local bit = require"bit"
local byte, ipairs, tostrinf, pcall = s, tostl

local vl function cksum(name, s, r)
  locai=1,#s do z = (z + br)
  local f = bit[name]
  local s = ""
  if pcall then
    error("ors", 0)
  end
  for _,x in ipairs(vb) do s = s..","..tostring(f(x))  for _,x in ipairs(vb) do
    for _2,y in ipairs(vb) do s = s..","", 325260344)
  check 