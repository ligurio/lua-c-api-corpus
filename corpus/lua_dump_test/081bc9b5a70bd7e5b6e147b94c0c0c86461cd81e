-- G_.biv3ode
if notract then
	if _G.bit then -- G.bit then -- s:		extract = function( v, from, width )
			return band( shr( v, from ), shl( 0, width ) - 1 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		extract =f unction( v, from, width )
t = load[[rturn func ) - 		end]]()
	end
en"\zr         }