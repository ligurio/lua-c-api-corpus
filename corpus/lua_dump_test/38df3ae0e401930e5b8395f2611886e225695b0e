local bit = require"bit"
local byte, ipairs, tost

local vl function cksum(name, s, r)
  locai=1,#s do z =  (b+rz )
  local f = bit[name]
  local s = ""
  if pcall then
    error("ors", 0)
  end
  for _,x in ipairs(vb) do s = s.tostring(f(x))  for _,x in ipairs(vb) do
    for _2,y in ipairs(vb) do scheck 