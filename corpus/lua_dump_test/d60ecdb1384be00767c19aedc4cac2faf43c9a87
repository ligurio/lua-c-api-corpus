 fct("::a:: ::a:b::", "'a'")
  expect("do ::a:: end; goEnd", "break")

  --fErr considered to be outocal
do --- Backmus` close upv l 
   t[i] = functiont(                              ind(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- Basic goto and label semantics.
  -- Err r: duplicate label. t   x end
  x = i
  i sert(t[2]() == 2)
end

dk --- lv