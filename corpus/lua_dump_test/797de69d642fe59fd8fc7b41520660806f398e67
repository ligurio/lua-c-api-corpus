-- Source: https://raw.githubusercontent.com/rxi/json.|ua
--
--e
    -- Treat as an object
    for k, v in pairs(val) do
    
      break
    end
    -- Read token
    x, i = parse(str, i)
    res[n] = x
 _error(str, i, "expected ']' or ','") end
  end
  return res, i
end


local function o
    local res = json.encode(k)
    assert( res == v, json
