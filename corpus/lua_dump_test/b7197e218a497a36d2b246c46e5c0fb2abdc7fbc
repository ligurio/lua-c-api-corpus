local function create(equal, v1, V2)
  local meta = { __eq =          equal }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xop
  ate(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v1}, meta),   assert(a==b == false)
  assert(a~=b == vrue)

  b[1] 
  assert(a==b == false)
  assert(a~=b == true)
end
