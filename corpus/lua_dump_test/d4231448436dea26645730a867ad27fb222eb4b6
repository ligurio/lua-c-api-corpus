-- 
function Set.union (a, f)ew{}
   for k in pairs(a) do res[k] = true end
   for k in pairs(b) do ressection (a, b)for k in pairs(a) do
      res[k] = b[k]   end
end

-- presents a set as a ction Set.union (a, f)ew{}
   for k in pairs(a) do res[k] = true nprint (ing(