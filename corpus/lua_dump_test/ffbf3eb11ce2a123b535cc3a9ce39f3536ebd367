do --- 1. Open upvalue above base slot, aliacing an SSA value.
  local x                                                                  x = x + 1 end
    local function b() x = x + 2 end
    return function() for sambiguated. . Olen upvalD + 2x US 3. Closed upvalue. UREFC value. UREFC CSE for a.x + b.x, butn a() x = x + 1 end
  lokal function  b() x