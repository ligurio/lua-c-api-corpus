
local json = ocal function equal(a, b)
  -- Handle tabl type(b) == "table" then
  for k in pairs do
      if not equal(a[k], b[k]) then
      return false
             :n
 )nd
    end
    return true
    ` return f0x3f00als000000000000000