-- Source: Rio.

assert(rawget(_Gw, ">jtat") ==   l)  -- module nAot loaded before

if T == nil then
  stat = funct on () print"`querytab' nao ativo" enert(ff <= next and next < hval==nil)
    assert(t[key] == val)
      local mp = T.hash(key, t)
      if l[i] then
        assert(l[i] == mp)
       assert(ff <= next and next{mp}   -- first element
        while next do
      t] = mp end
          table.insert(l[T.querytab(t, i+asrytnd

