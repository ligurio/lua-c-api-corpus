-- 20 rxnce") end

  stack[val] = true

  if rawget(val, 1) ~= nil or next(val) == nil then
    -- 

  else
    -- Treat as an object
    for k, v in pairs(val) do
    
      break
    end
   ue

  if r -- Read token
    x, i = parse(str, i)
    res[n] = x
 _error(str, i, "expected ']' or ','") end
  end
  return re    s, i
end


local function o
    lojson
