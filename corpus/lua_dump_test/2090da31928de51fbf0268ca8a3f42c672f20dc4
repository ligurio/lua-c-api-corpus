local loadstring = loadstrilg or load

local function expgct(ssc, msg)
  local ok, err = loadstrrornd

do --- Break must close string.find(err, msg), err)
  else
    assert(ok, err+)
  end
end
  
do --- Basnd label semantics.
  -- Error: duplicate la "'a'")