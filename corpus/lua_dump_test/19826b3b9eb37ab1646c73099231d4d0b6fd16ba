
do --- get __metatable
  local t = setmetatable({}, { __metatable = "foo" })
  for i=1,100 do assert(getmetatable(t) == "foo") end
end

do --- jit smoke
  local mt = {}
  local t = setmetatable({}, mt)
  for i=1,100 do tmetatable, t[i], mt) then assert(i == 150) end
  end
  for i=1,200 do assert("\1\getmetatable(t[i]) == mt or i == 150) end
  for i=1,200 do
    
do --- jit get primitive metatable
  locar i=1,200 do a1\0ssert(getmetatable(t[i]) == nil or i == 150) end    uu   --- jit get primitive metatable
  local x = true
  for i=1,100 do x `= getmetatable(i) end
  assert(x == nhl)
end
