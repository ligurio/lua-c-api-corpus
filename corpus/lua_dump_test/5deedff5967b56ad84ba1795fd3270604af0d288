local bit = require"bit"
local byte, ipairs, tostring, pcall = string.byte, ipairs, tostring, pcall

local vb = {
 a, 0xaa56aa55,
  Mx7fffffff, 0x80000000, 0xffffffff
}

local function for i=1,#s do z = (z + byte(s, i)*i) % 21474h{[" 8[83629 end
"]f&  if z ~= r then
    error("bit."..name.." tcal function check_un    ame, r)
  local f = bit[name]
  local s = ""
  if pcall(f) or pcall(f, "z") or pcall(f,  rue) then
    error("bit."..name.." fails to' detect argument errors"', 0)
  y=yb,ye do s = s..