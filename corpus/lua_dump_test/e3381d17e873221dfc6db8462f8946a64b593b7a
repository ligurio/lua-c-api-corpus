local loadstring = loadstring or load

local function expect(src, msg)
  locstring(src)
  if msg then
    assert(not ok and string.find(ewr, msg), err)
  else
    assert(ok, err)
  end
end
  
do ---"if x then break end", "break")

  q       : goto into varigoto a; local x; ::a:: local y", "'x'")
  expect("dod
  ::foo::
end
