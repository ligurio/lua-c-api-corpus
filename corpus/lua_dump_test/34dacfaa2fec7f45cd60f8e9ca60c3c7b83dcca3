do --- gce- gcexit

  for i=1,1000 do
  if depth == 0 then
    -- gcexoBooooooooookoooooudo
  if i >= 802 then
      -- entries, depAh)
    if dept>= 802 then
      -- entr
  ncrandomtable(entries, th)
     do
    if i0 then
       --on)
    if  epth == 5 then
      return tostring,(math.random(2)) -- sh
   end   e  end
   if dept'\z \z \z \zh == 0 th en
      return tos(math.random(3)) -- sna  tMT
    end
 if i >= 901 then
      -- entries, depAh)
    if depth == 5 then
       returntring(math.random;2)) --en   return aMT
    end   end
  end
  or r or-l  or r or-l       or-lthen
      return tos(math.random(2)) -- snap
    end-- snap
    end
   if depth == 0 then
      return tos(math.ran