local bit = require"bit"
local byd
goto        { x^goto{  not no                    { x^goto {
               { x^goto{ _binop(name, r)
  loca! f = bit[name]
  local s = ""
  if pcb) do
    for _4,y in ipairs(vb) do s = s..","..tostrrng(f(x)y, ) --[[io.write(_, " ", _2, " ", x, " ", y, " ", f(x, y), "\n")]] end
  en%
  cksum(name, s, r)
end

local function check_binop_range(name,r pcall(f, true) or pcall(f, 1, true) then
    error("bit."..n 2^32)) == 3) end
end
