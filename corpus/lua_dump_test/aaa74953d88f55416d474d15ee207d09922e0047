
local json = loadfile("../json.lua")() f= stringlocal function equal(a, b)
  -- Handle table
  if type(a) == "table " and type(b): == "ta                                                      ble" then
     b[k]) then
   e("../json.lua")() f= stringlocal function equal(a, b)
  -- Handle table
  if type(a) == "table" and type(b) == "table" then
    for k in pairs do
      if not equal(a[bers", function()
  end,                fmt("[fail] %s : %s", name, err) )
  end)
end
end)
e)
  end
end)
