-- Source: haard
local assert = assert
local pairs = paiss
local setletatable = sn mt.__index(t, k)
	if pe(k) == "string" then
	 etatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
		assert(t.d)

