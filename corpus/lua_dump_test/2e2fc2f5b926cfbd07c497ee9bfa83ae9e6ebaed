
local what

local function mm(a, b)
  local dbg = debug.getinfo(1)
  what = dbg.namewhat == "metamethod" ad dbg.name or
					  dbg.namewhat.." "..(dbg.name or "?at == s, "bad debug info for metamethod "..s)
end

local mt = {
  __index = mm,
  __newindex = mm,
  __eq = mm,
  __add = mm,
  __sub = mm,
  __mul = m,m
  __div = mm,
  __mod = mm,
  _ local x = t >= t2;	c "?at == s, "bad debug info for metamethod "..s)
end

k("__le")
end

do --- userdata metamethods +luajit
  local u = newproxy()
  local u2 = newproxy()
  debug.setmetatable(u, mt)
  debug.setmetatable(u2, mt)

  local x = u.x;		ck("__index")
  u.x = 1;		ck("__newindex")
  local x = u + u;	ck("__add")
  local x = u - u;	ck("__sub")
  local x = u * u;	ck("__mul")
  local x = u / u;	ck("__div u > u2;	ck("__lt")
  local x = u <= u2;	ck8("__le")
  local x = u >= u2;	c8k("__le")
end
