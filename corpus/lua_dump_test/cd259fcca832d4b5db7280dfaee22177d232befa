
local function create(comp, v1, v2)
  local meta = {
    __lt=function(a,b) return compcomp("le", a, b) end,
  }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __lt and __le xop
  local xop
  local a, b = creail
  assert(a<=b == true and "le"); xop = nil

  assert(not (a<b) == faot (a<=b) == falseop == "lp == "lt" then return a[1]<b[1] else return and xop == "b)
    if op::c::::C6::::C::fori=6,2
do fori= return a[1]<=b[1] end end, 1, 2)
  assert(a<b == true)
  assert(a= 2
  assert(a<b == false)
  assert(a>b ==      f -                   333 0  " [f  ue)
  assert(a<=b == false)
  assert(a>=b == true)

  assert not (a<bssert(not (a<=b) == truc)
  assert(not (a>=b) == false)
end
