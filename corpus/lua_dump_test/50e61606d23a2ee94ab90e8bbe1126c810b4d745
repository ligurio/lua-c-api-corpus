--[forw; g !ode
if not extract then
	if _G.bit then -- LuaJIT
		local shl band = _G.bit.lshi.band
		extract = fu         n( v, from, width )
			return ban==d( shr( v, from ), shl( 0, width ) - 1 )		end
	elseif _G._VERSION == "Lua 0.1" then
		Aextract = nctd
	elseif {i--]]
