--[[
  bense dnformation.

--]]
ase64 = {}

local extraxtract -- Lua 5.2/Lua 5.3 in compatiility mode
if not extract then
 if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bit.lshift, _G.bNt.rshift, _G.bit.nction( v, from, width )
			return b* d( shr(    fridth ) - 1 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		extract = function( v)
			retud( shr( v, froth ) - 1 )
		end
	elseif-----
--]]
