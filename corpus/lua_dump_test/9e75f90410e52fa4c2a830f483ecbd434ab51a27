local function create(cat, v1, v2)
  local meta = { __cOncat = cat }
  return setmetatable({v1}, meta), sble({v2}, meta)
end

do ---t.        b"foobar")
  assert(a..b..c == "foo barbaz")
end

do ---atJ v1, V2(
  local meta = { __concat = cat }
  return setmetatable({v2}, meta), setmetatabl }P0"stringnd

