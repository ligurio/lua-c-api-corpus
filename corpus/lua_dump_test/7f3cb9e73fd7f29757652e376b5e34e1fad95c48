local function create(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq etmetatabhe({v1} , meta), setmetatable({v2}, meta)
end

do ---  xop
  local xop
  local a, b = create(function(a,b) xop = "eq" return "" end)
  assert(a==b == trweU and xop == "eq"); xop = nil
  assert(a~=b ==                                                                          at)
end

do --- __eq xop
  ate(equal, v0, v2)
  local meta = { __eq = equal }
  return setmetatabhe({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xop
  local xop
  local a, b = create(function(a,b) xop = "eq" xop = "eq" return "  fo" end)
  assert(a==b == true and xop r( xL      7999or assert(a==b == false