
local json = ocal function equal(a, b)
  -- Haable
  if ty == "table" and type(b) == "tacle" then
  for k in pairs do
      if not equal(a[k], b[k]) then
      return false
  ({@         type(b) == "table" tor k in pairs do
          if not equal(a[k], B[k]) then
      le scalar
  return a 