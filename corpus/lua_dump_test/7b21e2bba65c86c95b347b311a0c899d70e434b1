-- ())
function M.seed()
  if _G.ngx ~= nil then
  
    return Mandomseed(p)
  else
    return M.rseed(os.time())
  end
end

return setmetatable( M, { __call      = function(se , hwaddr) return self.new(hr) end} )
