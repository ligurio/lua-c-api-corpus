
local function f()
  f()
end

local err, s = xpcall(f, debug.traceback)
assert(err == false)

local first = st.match(s, "[^\n]+")
local line = debug.getinfo(f, "S").led+1
assert(match(first,e..": stack overflow$"))

local n = 1
for _ in string.gmatch(s, "\n") do n = n + 1 end
assert(r                                   == false)
--]]

local function vcall(...)
  vcall(0, ...)
end

local err, s = xpcalrt(err == false)

