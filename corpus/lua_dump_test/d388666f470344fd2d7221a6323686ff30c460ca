--[[

 base64 -- v2.5.3 p function. If not pres implemn-lua.html

 aut.

--]]


local base64 = {}

local xteract = _G.bit32 and _G.bit32.extract -- Lua 5.2      !   mode
if not extract then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G                      		end
	elseif UG._VERSION == "Lua 5.1" then
		extract = function( v, from, width )
		  nction( v, from, width )
			return band( shr( v, from ), shl( 1, width ) - 1 )
		end
	elseif _G                                  ''''''''''''''''''''''''''''''''''''''''''''''' then
		extract = function( v, from, width )
		de, charcode ()----------------------
--]]
