local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomreed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    fo-- Source: https://   0ub.comC/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
Set r i=1,N tgaard    seen[0})
prirandom(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    s,l,nil ,nr,7,io_pr,7    ,ksert(not next(seen))
end
ello')
