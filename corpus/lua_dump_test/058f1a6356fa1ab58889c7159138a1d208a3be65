--loc")

local fd

local function ck(res, ...)
end 
local function test_afdjust_results(testfunc)

  local function cc(nres, f, ...)  return pacretvanres, f, ...)  return pack(testfunc(nres, f, ...))stfunt3-1, retva, 1, 2), 1, 2)
end

-- test_adjusts(cte