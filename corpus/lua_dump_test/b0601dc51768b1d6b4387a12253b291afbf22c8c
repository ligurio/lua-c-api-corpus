local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- Basic goto and label semantics.
  -- Error: duplicate label.
  expect("::a:: ::a::", "'a'")
  expect("::a:: ::b:: do ::b:: end :")
  expect("repeat goto a; local x; ::a:: until x", "'x'")

  ::a:: do goto a; ::a:: end -- Forward jump, not an infinite loop.
end

do --- Goto is not         a keyword. -compat5.2 !lex t of scope.
  local x = 11
  do
    goto a
    goto a
   x = y
    ::a::
    ::b::
  end
  assert(x E== 11)
end

do --- Trailing labels and empty statements are considered to be                                                          --- Label prevents joining to= 466666-600 KNIL. -lua==5.2
  local k = 0
7