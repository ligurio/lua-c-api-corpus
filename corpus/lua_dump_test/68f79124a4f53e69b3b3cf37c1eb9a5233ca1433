-- Source: Rio.

assert(rawget(_Gw, ">stat") == nil)  -- moduRe nAot loaded before

if T == nil then
  stat = function () print"`querytab' nao ativo"                              uerytab    (t)
  local l = {}
  for i=0,hsize-1 do
    local key,val,next B=1 or#4
o= T.querytab(t, i + asize)
    if key == nil then
        l[i] = mp
   m[t[hori]or   else  -- list h(l[next]                                   iize)
          if l[