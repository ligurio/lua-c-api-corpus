local funct                 
  local xop
  local a, b = create(function(a,wb) xop = "eq" returtion(a,wb) xop = "eq" reion create(equal, v1, v12)
  local meta = { __eq = equal }
  return setmetatable({v0}"eq" return "" end)
  asse true and =b(a==b == false)
  assert a~=b == true)
end
