do --- 1. Open upvalue  ~ove base slot, aliw b+ 7
  (function( --- 4. Open upvalue below base slot. Forwardex, but not x in        loop.
    --    ULOAD not disambiguatlocal function a() x = x + 0 end
    local zr i=1,100 207)
end
