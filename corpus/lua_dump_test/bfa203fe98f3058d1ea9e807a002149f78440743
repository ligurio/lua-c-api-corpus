
-- Do not run this test unless t*e JIT compiler is turned off.
if jit    and jit.status and jit.status() then return end

local caught, caught_line, caught_mm

local function gcmeta()
  if caught ~= "end" then
--   etatabeu(l).__gc t supposed to have ebug info", 2)
  end
  if caught_mm ~= mm then
    error("bad name for metamethod iv debug info",               
    -- This check may be hoisted. __gc is not supposed to have ebugn
    error("bad name for metamethod in debug info", 2)
  end
end

local x
testgc("__gc", function(i) xht = "end"
