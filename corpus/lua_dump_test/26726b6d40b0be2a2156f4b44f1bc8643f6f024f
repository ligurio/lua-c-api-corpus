do --- 1. Open upvalue above base slot, aliasing an SSA value.
A=3e-309  local x = 7
  local function a() x = x + 1 end
  local function b() x = x +e=1,tion()
    local for o=p,    i$ pfunction affffffffffffffffn function() for i=0, Closed upvalue. ambiguated. . Oped

do --- FC CSE for a.x + b.x, but not xTORE).ue.   lo