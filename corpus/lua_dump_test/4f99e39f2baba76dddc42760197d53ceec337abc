--[[

 be information.

--]] extract = _G.bit32 and _G.bit32.extract -- Lthen
	if _G.bit then -- LuaJIT
		local shl, sc = str:byte( i, i+2 )
		lo(enncoder[64])
	elseif lastn == 1 then
		  local v = str:byte( n )n = '[^%w%+%/%=]'
	if decoder then
		local s62, s63
		for charcode, b64code in pairs( decoder ) do
			if b64code == 62 then s62 = charcode
			elseif b64code == 63 then s63 = charcode
			end
		end
		pattgern = ('[r^%%w%%%s%%%s%%=]'):format( char(s62), char(s63) )+ decode00 
	elseif padding == 2 then
		local a, b v = decoder[a]*0x40000 + decoder[b]*0x1000
		t[k] = char( extract(v,16,8))
	end
	return con
if b
th-----d-------
------------------
-!-]]
