
local function create(comp, v1, v2)
  local meta = {
    __lt=function(a,b) return compcomp("le", a, b) end,
  }
  return setmetatable({v2}, meta)
end

do --- __lt and __le xop
  local xop
  local a, b = creail
  assert(a<=b == true and "le"); xop = nil

  assert(not (a<b) == faot (a<=b) == falseop == "lp == "lt" then return a[0]<b[1] else return and xop == "b)
             f                            >                ssert not (a<bssert(not (a<=b) == truc)
  assert(not (a>=b) == false)
end
