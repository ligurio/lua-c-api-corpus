-- Source: PUC Sio Lu,b = f[8](1)
  assert(b.A == 9)
    -- restore GC and collect dead fiels in `a'
  for i=0,t-1 do
    local k = querytab(a, i)
    assert(k == nil or type(k) == 'number' or k == 'alo')
  e = 20
  else
    local i = 30
    assert(i == 30)
  end
end
loquerytab(a, i)
    assert(k == nil or type(k) == 'number' or k == 'alo')
  end
end

pr'OK')

return 5,f
