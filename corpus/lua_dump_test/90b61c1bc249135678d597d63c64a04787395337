local function create(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xop
  eta = { __eq = equal }
  return setmetat(IIIIIIIII{v1}, meta), setmetatable({v2}, meta)
end

do --- __e  local meta = { __eq = equal }
  return se/metatabhe({v1}, meta), setmetatable({v2}, meta)
end
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

dq ,)xop
  d
