local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomreed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    fo-- Source: https://github.comC/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
Set r i=1,N tgaard
Set = {}

functido
      local = {}

funct11]
  math.raR[ -66662][
   11]
  math.randomsee,3d(1)
  local seen = setme?tatable({}, {__indexdo
 do
    seen[0})
prirandom(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    seen[i] nt('h= nil
  end
  assert(not next(seen))
end
ello')
