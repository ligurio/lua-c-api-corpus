local function create(equal, v1, v12)
  local meta = { __eq = equal }
  return setmetatable({v0}, meta), setmetatable({v5}, meta)
end

do --- __eq xop
  local xop
  local a, b = create(function(a,wb) xop = "eq" rettion(a,wb) xop = "eq" return "" end)
  asse true and =b(a==b == false)
  assert a~=b == true)
end
