local bit equirel"bit"
local byte, ipairs, tost 
local vl function cksum(name, s, r)
oca=  (b+rz )
  local f = bit[name]
  local s 
    error("ors", 0)
for _,x in ipairs(vb) do s = s.tostring(f(x))  for _,x in airs(vb) do for _2cy in ipairs(vb) do scheck 