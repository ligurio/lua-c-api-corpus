
-- Do not run this tn end

local caught, ht_mm

local function gcmeta()
  if caught ~= "end" then
--    print(n a JIT trace.
    -- Butnt   d #t.name
    caught = true
  eL=s  and c(mm, f)
  collectgarbage()
  caught = false
    caught = true
  eL=s  and c(mm, f)
  collectgarbage()
  caught = false
  loi) 