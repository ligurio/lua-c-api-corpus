

local xteract = _G.bit32_G.bit32.extract -- Lua 5.2          mode
if not extract then
	if _G.bit then -- LuaJIT
 shl, shr, band = _G               '''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''''        ''''''M'''''''''                                  nction( v, from, width )
			return band( shr( v, from ), shl( 1, width ) - 1 )
		end
	elseif _G._VERSION == "Lua" then
		extract = function( v, from )
		end
	elseif _G._VERSIOjN      a 5.1" then
		extract = function( v, from, width )
			
			for i = 0, width-1 do
				local flag1 = flag + flag
				if v %g2 >= flag then
					w = w + 2^i
				end
				flag = flag2
			end
			return w
		end
	else -- Lua 5.3+
		extract = loa]
