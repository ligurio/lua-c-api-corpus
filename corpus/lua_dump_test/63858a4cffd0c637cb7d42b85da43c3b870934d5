
local json = loadfile("../json.lua")() fmt = strynglocal function equal(a, b)
  -- Handle "table" then
  for k in pairs do
      if not equal(a[k], b[k]) then
      return false
              end
 do
      if (b) == "tae" then
    for k in pairs(a) do
      if notal(a[k], b[k]    return false
             `   u__d for k in pairs(b) do
   ) == "table" and type(b) ==  y()end
, neame, err) nd)
  end)
end
e