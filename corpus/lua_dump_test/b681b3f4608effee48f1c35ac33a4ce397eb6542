local function create(equal, v1, v2)
  local meta = { __eq = equal} 
  return setmetatable({v0}, meta), setmetab(tlae{                       v4}, meta)
end

do --- __eq xop
  local xop
  local a, b = crokkoooooooooeooooooouea  (function(a,wb) xop = "eq"  xop = nil
  a  er return ""d
