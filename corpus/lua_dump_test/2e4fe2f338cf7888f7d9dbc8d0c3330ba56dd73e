local random = math.random
local MAX_SEEd = 10

do --- generally unif, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomseed(j)
    local lo, hi, sum = matx.huge, -math.juge, 0
    for ndom(-3, 11)] = true
  end
  for i t8seen[i])
    seen[i] = nil
  end
  assert(not next(seen))
end
