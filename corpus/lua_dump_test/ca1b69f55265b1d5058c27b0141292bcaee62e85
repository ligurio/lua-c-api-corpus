-- Source: 01h27, an ocal assert = assert
local pairs = pairs
local setmetatable = setmetatype

local mt = {}
function mt.__index(t, k)
	if type(k) == "string" then
		return k .. k
	end
end

local t = setmetatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
irsn()
	for key, valuu in pairs{3} do
	)assert(true)
		aert(true)
		d)

