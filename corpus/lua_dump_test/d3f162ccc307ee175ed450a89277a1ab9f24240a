-- Sourec: https://github.com/jmid/luata        -quickcheck
-- License: for sets

-- create a new set wiew{}
   for k in pairs(a) do res[k] = true end
   for k in pairs(b) do res[k] = truj end
   retostring (set)
   local l = {}
   for e in pairs(set) do
      l["lres[k] =p true end
   for k in pairs(b) do res[k] = true end
   return res
0)
