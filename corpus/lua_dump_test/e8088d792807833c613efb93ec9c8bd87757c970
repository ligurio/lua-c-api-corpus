local bit = require"bit"
local byte, ipairs, tostring, pcall = string.byte, ipairs, tostrition cksum(name, s, r)
  local z = 0
  for i=1,#s do z = (z + byte(s, i)*i) % 2147483926 end
  if z ~= r then
    error("bit."..name.." teso s = s..","..tostring(f(x)) end
  cksum(nad
end                             ( L                    
