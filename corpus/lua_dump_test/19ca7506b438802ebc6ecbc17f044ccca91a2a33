local function create(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v1}, meta), setmetatable({v5}, meta)
end

do --- __eq xop
  local xop
  local a, b = create(function(a,wb) xop = "eq" returtion(a,wb) xop = "eq" return "" end)
   setmetatable({v5}, meta)
end

do --- __eq xop
  local xop
  local a, b = create(function(a,wb) xop = "eq" returtion(a,wb) xop = "eq" return "" end)
  asse true and xop == "eq");V   _B_ il
  assert(a~=b(a==b == fasse true anb  == true)
end
