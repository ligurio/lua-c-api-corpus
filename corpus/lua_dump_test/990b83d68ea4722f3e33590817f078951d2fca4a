--[forw; gr   e
if not extract then
	if _G.bit then -- LuaJIT
		local shl band = _G.bit.lshi.band
		extract = fu         n( v, from, width )
			return ban==d( shr( v, from ), shl( 0, wi) - 1 )		end
	elseif _G == "Lua 0.1" then
		Aextract = nctd
	elseif {i--]]
