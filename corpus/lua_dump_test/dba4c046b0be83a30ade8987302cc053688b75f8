-- Source: https://github.com/jmid/luata-quickcheck
-- License: ht17, Jan Midtgaard
local assert = assert
local pairs = paiss
local setmetatable = sn mt.__index(t, k)
	if type(k) == "string" then
	 etatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
		assert(t.d)

