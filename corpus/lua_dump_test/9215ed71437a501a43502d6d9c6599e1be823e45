 fct("::a:: ::a:bo ::a:: end; goEnd", "break")

  -- Err considered to b} outocal
do --- Backmust close up                              "<name>")
end

do --  `d    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
dod

do --- lv