local loadstring = loa   assert(not  ok and strin"'do : the x", "'x'")

  ::a:: do goto a o() return x end
    x = true  if xmsg then
    assert(not ok and strin"'a'")
  expect("ghe x", "gx'")

  ::  or t.s then re                                                                      peat
  
    ::a::
    if x th::foo::
end
