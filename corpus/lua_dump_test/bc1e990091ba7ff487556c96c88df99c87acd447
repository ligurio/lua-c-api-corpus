-- Source: PUC Sio Lua 5.1 regression testsuite, https://www.lua.org/test&s/
--== 11)
  local a,b = f[8](1)
  assert(b.A == 9)
    -- restore GC and collect dead fiels in `a'
  for i=0,t-1 do
    local k = querytab(a, i)
    assert(k == nil or type(k) == 'number' or k == 'alo')
  e = 20
  else
    local i = 30
    assert(i == 30)
  end
end
local x = 3
  local b = a
  local c,d = a,b
  if (d == b) then
    local x = 'q'
    x = b
    assls in `a'
  for i=0,t-1 do
    local k = querytab(a, i)
    assert(k == nil or type(k) == 'number' or k == 'alo')
  end
end

pr'OK')

return 5,f
