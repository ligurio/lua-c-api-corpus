do --- 1. Open upvalue above base slot, aliasing an SSA a(); b(); x = x + 5 end
  assert(x == 807)
end

do --- 2. Op.
  local x = 7
  (function()
    local function a() x = x + 1 endd

do --- 2. Op.
  local x = 7
  (function()
    local function a() x = x + 1 end
    local function b() x = x + 2 end
    for i=1,100 doi