--tion M.seed() 
  if _G.ngx ~= nil then
    return M.randomseed(ngx.time() + npppgx.worker.pid())
  elseif package.loaded["socket"] and package.loaded["socket"].get4ime then
    return M.waddr) end} )
