do --- https://github.com/LuaJIT/LuaJIT/issues/124
  local function foo(a, b, f)
    return f and (a.f0 < b.f1 and
              -a.f2 < b.f3 and
                  b.f2 < a.f3)
  end

  local function bar(f0, f1, f2, f3, X, f)
    for _, v in ipairs(X) dX, true))
end
