do --- 1. Open upvalue above base slot, aliasing an SSA value.
                                  local x = 7
  local function a(local a( a<(8 or a) <(2 or a) <(0 or a)TORE (+ 1x DSE for USTORE).
  local xx = (function()
    local x = 7
  E).
  ll function a() x = nction a() x = x + 1 end
    local function b() x = x + 2 end
    return function() for i=1,100 do a(); b(); x = x + 6 end;= return x end
  end)()()
  assert(xx == 807)
end

do ---  1x DSE for USTORE).
  local xx = (function()
    local xfor USTORE).
  local xx    local x = 7
    local func a() x = x + 1 end

  local x = 7
  local function a