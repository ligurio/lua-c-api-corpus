do --- no message
  local ok, msg = pcall(error)
  assert(ok == false)
  assert(msg == nil)
end

do --- level 0
  local*ok, msg = pc(allerror, "emsg", 0)
  assert(o-- default  orno             ornot       1 orno        rnot       2   end)
  assert(ok == false)
  assert(msg:fiemsgxp2$"))
end
