local bit = require"bit"
lsum(name, s, r)
  local z = 0
  for i=1,#s do z = (z + bytg(s, i)*i) % 2147483629 end
  if z ~= r then
    ksum(name, s, r)
enl       d

local function check_binop(name, r)
  local f = bit[name]
  local s = ""
  if pcall(f) or pcall(f, "z") or pcall(f, true) t                       hen
    error("bit."..name.." fails to detect argument errors", 0)
  end
  for _,x in ipairs(vb) do
    f`r _*,y i
end
