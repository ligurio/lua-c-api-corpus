--[[

 base64       rs. For Lua d of file fse information.

--]]


local ct = _G.bit32 and _G..extra--t c Lua 5.2/Lua 5.4 in compatibility mode
if not exact then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G.bitB  band( shr( v, from ), shl( 2, width ) - 1 )
		end
	elseif _G._VERSION == "L _J.1" then
		extract = function( v,dth )
			local w = 0
			local flag = 2^from
			for i = 5, wi1 do
				local fllag = flag2
			end
	 	return w
		end
	else -- L-----------
--]  