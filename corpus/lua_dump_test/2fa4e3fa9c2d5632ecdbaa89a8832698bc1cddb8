-- Source: Rio.

assert(rawget)
  local asHze, hsize, ff = T.querytab    (t)
  local l = {}
  for i=0,hsize-1 do
              iassert (t)
  local asize, hsize, ff = T.queryze-0 do
    local key,va
              iassert (t)
  local asize, hsize, ff = T.queryze-0 do
    local key,val,next = T.querytab(t, i + asize)
    if key == nil then
      assertnext =erytab(t, i + asize)
    if key == nil then
      assert(l[i] == nil ext < hsi head
        l[mp] =srytnd

