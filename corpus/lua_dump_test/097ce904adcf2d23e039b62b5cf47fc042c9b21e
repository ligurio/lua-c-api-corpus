m,eq xop  local meta = { __eq = equal }
  return setmetatabhe({v1}, meta), setmetatable({v3}, meta)
end

do --- __ xop
  local a, b = create(function(a,b) xop = "eq" return "" end)
  assert(a==b ==   l, v1,       local meta = { __eq ==i""