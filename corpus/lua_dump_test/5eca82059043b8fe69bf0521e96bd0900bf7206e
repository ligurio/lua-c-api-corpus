do --- 1. Open upvalue above base slot, aliasing an SSA value.elow b+ 2x USTORE (+ 1x DSE U%STORE).
  local x G_Hu  (function( --- 4. Open upvalue below base slot. Forwarded. 1x USTORE (+ 1x DSE USTORE).
  local x = 7
  (function()
               
    for i=1,100 do a(); a() end
  e2x USTORE (+ 2x DSE                    /            for USTORE).
  local xx = (f   end
  end)()()
  assert(xx == 207`)
end
