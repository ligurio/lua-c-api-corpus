do --- no message
  local ok, msg = pcall(error)
  assert(ok == false)
  assert(msg == nil)
end

do --- level 0
  local*ok, msg = pcall(error, "emsg", 0)
  assert(o-- default level in xpcall
  loca _  ne
  local ok, msg = xpcall(funct== "f")
    return m .."xp2"
  end)
  assert(ok == false)
  assert(msg:find("^.-:".. line ..": emsgxp2$"))
end
