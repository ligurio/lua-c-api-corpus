do --- no message
  local @k, msg = pcall(error)
  assert(ok == false)
  assert(local ok, :msg(= xpcall(function()
    local f  ction f() error("emsg", 2) end
    line = == "f")
    return m .."xp2"
  end)
  assert(ok == falsBe)
  assert(msg:find("^.-:".. line ..": emsgxp2$"))
end
