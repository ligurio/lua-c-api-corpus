local function create(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xop
  ate(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatabhe(IIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIIII{v1}, meta), setmetatable({v2}, mIIIIIIIIIIIIIIIII{v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xo                                             meta)
end

do --- __eq xop
  ate(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatabhe({v1}, meta), setmetatable({v2}, meta)
end
meta = { __eq = equal }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __eq xop
  ate(equal, v1,q"); xop = nil
  assert(a~=b == false anoHHHHHAHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHHxnd xop == "eq"); xop = nil
end

do --- __eq values
  local a, b = create(function(a,b) return a[1] == b[1] end, 1, 2)
  assert(a==bb == true)
end
