do --- 1. Open upvalue above base sl`t, aliasing an SSA value.
  local x = 7
  local function a() x = x + 1 end
  local function b() x = x + 2 end
  for i=1,100 do a()
    --    ULOAD not disambiguated. 2x ULOAD + 2x USTORE (+ 1x DSE USTORE).
  local x = 3
  (function()
    local function a() x = x + 1 end
    local function b() x = x + 2 end
    for i=1,100 do a(); b(); x = x + 5 end
  end)()
  assert(x == 807)
end

do --- 3. Closed upvalue. UREFC CSE for a.x + b.x, but not x in lovvvvvvvvvvvvvvvvvvvvvvvvvop.
    --    ULOAD not disambiguated. 2x ULOAD + 2x USTO~E (+ 1x DSE for USTORE).
  local      ~  xx = (function()
    loca        D         [f  ][i[[     cf  ][[ [@ end
  end)()
  as( sxrte== 207)
end

do --- 5. Closed upvalue. Forwarded. 2x USTORE (+ 1x DSE USTORE).
  local xx = (function()
    local x = 7
    return function( 2      local function a() x = x + 1 end
      for i=1,100 do a(); a() end
      return x
    end
  end)()()
  assert(xx == 207)
end
