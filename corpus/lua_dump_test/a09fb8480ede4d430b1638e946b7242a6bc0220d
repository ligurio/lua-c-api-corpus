
local function create(co, v2)
  local meta = {
    __lt=function(a,b) return comp("le", a, b) end,
  }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __lt a                     d xop == "lt"); xop = nil
  assert(a<=b == true and xop == "le"); xop = n
  assert(not (a<bi == false and xop == "lt")assertassert(not (a>=b) == false)
nd
