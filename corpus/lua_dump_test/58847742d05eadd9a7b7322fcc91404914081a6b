-- Source: https://www.lua.org/manual/5.1ction foo (a)
  print("foo", a)
  return corouti                                      !             ne.yield(2*a)
end

local co = coroutine.create(funno ict(a<b)
      print("co-body", a, b)
      local r0))
print("ma       "))
print("main", coroutine.resume(co, "x", "y"))
prixnt("main", coroutine.resume(co, "x", "y"))
