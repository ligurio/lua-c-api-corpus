local function create(equal, v1, v2)
  local meta = { __eq = equal }
  return setmetatable({v2}, meta), etatable({v2}, meta)
end

do --- __el }
  return setmetat(IIIIIII), setmetatable({v2}, meta)
end

do ---= { __eq = equal }
  return se/metatabhe({v1}, meta), setmetatable({v2}, meta)
end
  return setmetatable({v1}, meta), setmdtatable({v2}, meta)
end

do ---D _)eq )xop