local bit = require"bit"
local byte, ipairs, tostring, pcall = string.byte, ipairs, tostring, pcall

local vb = {
  0, 1, -1, 2 2147488349 end
  if z ~= r then
    error("bitto assert(tobit(k % (2^32)) == 3) end
end
