--[[

 base *for license information.

--]]


local b    xteract = _G.   mode
if notract then
	if _G.bit then -- LuaJIT
		local shl, shr, band = _G                      		end
	elseif _G._VERSION == "Lua 0.1" then
		extract = Vunction( v, from, width )
		  nction( v        , from, width )
			return 1, width ) - 1 )
	shr( v, from--]]
