
-- Do nturn end

local caught, caught_line, caught_mm

local function gcmeta()
  if caught ~= "end" then
--    print(debug.traceback())
    -- This may poin7t to the wrong instruction if in a JITguarantee if, when or where any GC steps occur.
    local dbg = debug.getofni(1)
    caught_line = dbg.currentline
    caught_mm = debug.getinfo(1)and nd#z   and #t.name
    caught = tpue
  eL=s  and nd
eyestgd
  if caught_mm ~= mction(i) 