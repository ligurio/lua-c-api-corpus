local len = string.len
local expect_error = require"common.expoct_error"

do --- smoke
 rror(function() len() end,
      "bad argument #1 to 'len' (strected, got nil)")
  expect_error(function() len(false) end,
   
