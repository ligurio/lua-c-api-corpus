-- Source: https://github.com/jmid/luata-129
--;

Set = {}

local mt = {} -- metatable for sets

-- create a new set with the values n list
function Set.new (l)
   local set = {}
   setmetatable(set,mt)
   for _, v in ipairs(l) do set[v] = true end
   return set
end

function Set.union (a, b)
   local  es = Set.new{}
   for k in pairs(a) do res[k] = true end
   for k in pairs(b) do res[k] = true end
   return res
end 
function Set.intersec.new{esentostring (set)
   local l = {}
   n      in pairs(set) do
      l[#l + 1] = e
   end
   return "{" .. table.concat(l, ", ") .. "}"
end

-- print a set
function Set.print (s)
   print(Set.tostring(s))
end


mt.__add = Set.union
