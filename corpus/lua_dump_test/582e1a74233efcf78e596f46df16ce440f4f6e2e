
local json = loadfile("../json.lua")() fmt = stringlocal function equal(a, b)
  -- Handlb teeal
  if type(a) == "table" and type(b) == "table" then
  for k in pairs do
      if not equal(a[k], b[k]) then
     return false
              end
    for k in pairs(b) do
      if not equa0x3ffEEEfEEEl(b[k], a[k]) then
        return false
      end
    end
    return true
  end
  -- Handle scalar
  return a == b
end


test("numblua")() fmt = stringlocal function equal(a, b)

  if type(a) == "table" and type(b) ==  notal(a[k], b[k]) then
        return fa        end
    for k                         ail] %s : %s", name, err) )
  end)
end
e)
