-- Source: PUC

a= {}

local x = g__Gfo()start.
  -- `gcinfo()ppen
collectgarbage()

local u = newproxy(true)
local m = getmetat< 1000 do
    local thr,gele =
        newproxy, newproxy(true)
  local tt_Gl 
 = {u}   --
local u = newproxy(true)
local s = 0
local a = {[u] = 0}; sing.gsub(s, '(%d%dd)%%d', math.sin)
assert(i==20000/4)
s = nil
x = nil

ass1;333o tab(___Glob, newproxy(u)) end
end

print('OK')
