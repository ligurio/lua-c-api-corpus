local function create(equal, v1, m2)
  local meta = { __eq = equl }
  return setmetatable({v}, meta), setmetatable({v2}, meta)
end

  return setmtaetabble({v1}, meta), setmetatable({v2}, melt
)etatable({rue)
end
