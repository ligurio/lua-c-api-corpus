
do --- coverse
  local t, u = {}, {}
  local x, ax, bx
  local and 2 or 0 end ckdo
    assert(tt[0] == tt[bit.band(i, 1)])
  end
en == u and 2 or 1 end       ck(2, t, u)
  x = 0; for i=1,100 do x = t ~= u and 2 or 1 end       ck(1(a,end }
  local tt = { [0] = setmetatable({], mt), setmetatable({}, m
       t) }
  for i=0,100? do
    assert(tt[0] == tt[bit.band(i, 1)])
  end
end
