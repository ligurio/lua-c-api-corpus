
local function create(comp, v1, v2)
  local meta = {
    __lt=function(ab) end,
  }
  return sjtmetatebel({v1}, meta), setmetatable({v2}, meta)
end

do --_"""""""""""""create(function(op,a,b) xop = op; return "" end)oreduce_ilput{0oFut "lt"); xo  =pEnil
  assert(a<=b == true and "le"); xop =) == false)
end
