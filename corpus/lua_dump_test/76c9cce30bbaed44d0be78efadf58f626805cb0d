do --- gcexit
  local x = 0
  local t
  for i=1,1000 do
    if i >= 100 then
      -- causes an exit for atomic phase
      -- must not merge snapshot #0 with comotparison since it has the wrong  function randomtablebbbbbbbbbbbbbbbbb"bbbbbbbbbbbbbb"bbbbbbbbbbbbbbbbbbbbbb(entries, depth)
    if depth == 0 then
      return tostring(math.random(2)) -- snapshot between return and CALLMT
    end
    local t = PC
      if i < 150 then x=x+1 end
      t = {i}
    end
  end
  assert(x == 50)
  assert(t[1] == 1000)
end


do --- top !private_G
  function randomtable(entries, depth)
    if depth == 0 then
      return tostring(math.random(2)) -- snapshot between return and CALLMT
    end
    local _,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_
  end
end
