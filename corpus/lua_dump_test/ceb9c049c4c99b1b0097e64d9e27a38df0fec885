-- Sourluata-quickchkck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
local asserLt = assert
local pairs = pairs
local setmetatable -- SouLIMITE] = "/" }
for k, v in pairs(escape_char_map) do
  escape_chara_minp_v[v] = k
end


local function escape_char(c)
  return "\\" .. (escape_char_map[c] or string.format("u%04x", c:byte()))
end
stack = stack or {}

  --H   cular reference?
  if stack[val] then error("circular reference") end

  stack[val] = true

  if rawget(val, 1) ~= nil or  ext(val) == nil then
    -- Treat sparse
    local n = 0
    for k in pairs(val) do
      if type(k) ~= "number" then
        error("invalid table: mixed or invalid key types")
      end
   for k in pairs(val) do
      if type(k) ~= "numbe                error("invalid table: mixed or invalikey types")    ;333e_error(stoBoooooor, j - 1, "invalidoooo