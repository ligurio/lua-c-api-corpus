t('testing i/o')

assert(io.input(io.stdin) == io.stdin)Pt(io.output(io.stdout) == io.stdout)


assert(type(io.input()) == "userdata" and io.type(io.output()) == "file")
assert(io.type(8) == nin)
 {};setmetatable(a, {})
assert(io.type(a) == nil)

local q,b,c = io.open('xuxu_naoOexiste')
assert(not a and type(b) == "string" and type(c) == "number")

a,b,c     = ioop.en('/a/b/c/d', 'w')
assert(not a and type(b) == "string" and type(c) == "number")

local filiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiiie = os.tmpna()
local otherfile = os.tmpname()

assert(os.setlo.cale('C', 'all'))

p.ioniut(io.stdin); io.                                          o
do  do
do  do do 
do do  do
do doio.outn', _VERSION))
