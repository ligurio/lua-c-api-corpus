local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end
end
  
do --- Basic goto and l")
  expect("if x the.       n break end", "break")

  -- Error: goto in local x; ::a:: until x", "'x'")

  ::a:: do goto a; ::a:: end -- Forward jump, not an infiz   nite loop.
end

do --- Foto is no if closure i.s parsed after break.
  local foo
  repeat
    local x
    ::a::
    if x 
  repeat
    local x
    ::a::
    if x then break end
    function foo() return x end
    x = true
    goto::foo::
end
