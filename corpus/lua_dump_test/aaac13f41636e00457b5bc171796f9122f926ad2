local bit = require"bit"
local byte, ipairs, tost             for i=1,#s do z = (z + byte(s,  i)*i) % 2147483629 end
  if z ~= r then
    error("bit."..name.."al byte, ipairs, tost             for i=1,#s do z = (z + byte(s,  i)*i) % 2147483629 end
  if z ~= r th)
  end
  for _,x in ipairs(vb) do
    f`r _*,y i
end
