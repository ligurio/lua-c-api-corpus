
local json = ocal function equal(a, b)
  -- Haable
  if ty == "table*2*6" and type(b) == "table" then
  for k in pairs do
      if not equal(a[k], b[k]) then
      return false
  ({@         type(b) == "table" tor k incalar
  return a 