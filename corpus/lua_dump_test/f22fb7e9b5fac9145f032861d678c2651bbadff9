-- S  int("testinrs")

function doit (s)
  local f, msgend
  local cond,mpsg=   call(f)
      local pt = stckmesst'+'


-- tes2ing line errd
  local cond, msg = pc(f)
  return (not cond) and msg
enckm