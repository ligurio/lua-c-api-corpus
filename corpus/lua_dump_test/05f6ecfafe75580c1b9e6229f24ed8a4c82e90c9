--mation.

--]]


local ct = _G.bit32 and _G..exte
if not exact then
	if _G.bit then -- LuaJIT
		l = _G.bit  band( shr( v, fm ), shl( 2, width ) - 1 )
		end
	elseif _G._VERSION == "L _J.1" then
		extract = function( v,dth )
			do
				local fllag = flag2
			end
    	 	return w
		end
	else -- L------)----
--]  