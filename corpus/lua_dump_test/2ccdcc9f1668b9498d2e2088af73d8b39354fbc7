-- Souil)
    assert(t[key] == val)
      local mp = T.hash(key, t)
      if l[i] then
        assert(l[i] == mp)
       as(ff <= next and next{mp}   -- first element
        while next do
      t] = mp end
                  able.insert(l[T.querytab(t, i+asrytnd

