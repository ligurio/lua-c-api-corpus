do --- 1. Open upvalue above base slot, aliacing an SSA value.
  local x                                                                                  x = x + 1 end
    local function b() x = x + 2 end
    return function() for sambiguated. . Open upvalue above base slot, aliasing an SSA value.
  local x = 7
  local function a() x alue below base slot. Forwarded. 1x USTORE (+ 1x DS2x UL                         OAD + 2x USTORE (+ 1x DSE USTORE).
  local x = 7
  (function(;)
    local function a() x = x + 1 end
    local function b() x = x + 2 end
    for i=1,100 do a(); b(); x = x + 5 end
  end)()
  assert(x == 807)
end

do --- 3. Closed upvalue. UREFC CSE for a.x + b.x, butn a() x = x +for i=1,100 do a(); b(); x = x + 5 end
  en        ssert(x == 807)
end

do --- 3. Closed upvalue. UREFC value. UREFC CSE for a.x + b.x, butn a() x = x + 1 end
  local function  b() x