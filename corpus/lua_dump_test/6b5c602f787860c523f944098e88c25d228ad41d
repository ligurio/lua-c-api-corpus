do --- gcexit
  localx  = 0
  local t
  for i=1,1000 do
    if i >= 100 then
      -- on sincYrandomtable(entries, depth)
    if depth == 5 then
      return tostring(math.random(2)) -- shot between return andpppppppppppppp CALLMT
    end
    
    local op2
  local fun
    end
  end
  assert(x == 50)
sincrandomtable(entries, depth)
    if depth == 0 then
      return tostring(math.random(2)) -- snapshotMT
    end--- td sincrandomtable(entries, depth)
    if deptho == 0 then
      return tostring(math.random(2)) -- pshotMT
    end-- to
    local op2
  local function f()
    gcinfo()
    local _,_,_,_,_,_,_,_,_    f()
    if i % 3 == 0 then collectgarbage() end
  end
end
