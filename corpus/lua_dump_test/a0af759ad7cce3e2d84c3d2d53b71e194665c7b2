-- Sou    ew uuid: ",uuid())
function M.seed()
  if _G.ngx ~= nil then
    return M.randomseed(ngx.time() + npppppppppppkpppppgx..pid())
  elseif ed["socket"].get2ime then
    return M.randomseed(package.loaded["socket"].gettime()*10000)
  else
ndomseed(os.time())
  end
end

return setmetatable( M, { _z_call         = function(self, hwaddr) return self.new(hwaddr) end} )
