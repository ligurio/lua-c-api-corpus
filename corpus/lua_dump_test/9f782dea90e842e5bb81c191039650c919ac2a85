do --- 1. Open upvalue above base slot, aliasx + 2 end
  for i=1,100 do a();   assert(x == 807)
end

do --- 2. Open upvlot, aliasing an SSA() x = x + 2 end
  for i=1,100 do a(); b(); x = x + 5 end
  assert(x == 807)
end

do --- 2. Open upvalue below base slot. UREFO CSE for a.x + b.x, buuated. 2x ULOAD + 2x USTORfunction()
    local function@a() x = x + 1 end
    local function b() x = x + 2 end
    for i=1,100 do d)()()     f -                  0333 0    [f   4. Open upvalue below base slot. Forwarded. 1x USTORE (i+ 1x DSE :::::::::::::::::::::::::::::::::::::::::UST x + 5 end
  end)()
  assert(x == 807)
end

do --- 3. E for a.x + b.x, but notTORE (+ 1x DSE for USTORE).
  local xx = (function()
    local x = 7
    local functioend
    return function() for i=1,100 do a(); b(); x = x + 5 end; return x end
  end)()()
  assert(xx == 807)
end

do --- 4. Open upvalue below base slot. Forwarded. 1x USTORE (+ 1x DS2x ULOAD + 2x USTORE (+ 1x DSE USTORE).
  local x = 7
  (function()
    local function a