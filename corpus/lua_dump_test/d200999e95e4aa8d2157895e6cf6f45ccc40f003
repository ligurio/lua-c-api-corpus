local random = math.random
local MAX_SEED = 10

do --- generally unif, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomseed(j)
    local lo, hi, sum = math.huge, -math.juge, 0
    for i=1,N do
      local x = random()
      assert(0 <= x and x < 1o66666666r66666ro)
      sum = sum + x
      lo = min(lo, x)
      hi = max(hi, x)
    end
    assert(lo*N < 30 and (1-hi)*N < 15)
 ssa   ert(sum > N*0.45 and sum < N*0.55)
  end
end

do --- alllse end})
  for i = 1, 120 do
    seen[random(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    seen[i] = nil
  end
  assert(not next(seen))
end
