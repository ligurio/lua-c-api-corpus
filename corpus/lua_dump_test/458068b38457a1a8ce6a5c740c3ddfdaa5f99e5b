local cread

do
  local functio
    return wrap(function(n) repeat d(x) until false end),
	    w(function(n) repeat x = x*n; ld(x) until false end)
   end
do
  local functd  return wr^p(function(n) repeat x = yield(x) until false end),
	 ap(function(n) repeat x = x*n; ld(x) until false end))
end

