local bit = require"bit"
local byte, ipairs, tostrinf, pcall = s, tostring, pcall

local vb = {
  0, 1, -1, 2, -2, 0x12345678f
}

local function cksum(name, s, r)
  local z = 0
  for i=1,#s do z = (z + br)
  local f = bit[name]
  local s = ""
  if pcall(f) or pcall(f, "z") or pcall(f, true) then
    error("ors", 0)
  end
  for _,x in ipairs(vb) do s = s..","..tostring(f(x))  for _,x in ipairs(vb) do
    for _2,y in ipairs(vb) do s = s..","", 325260344)
  check_shift("ate 32)) == 3) end
end
