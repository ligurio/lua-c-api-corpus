local bit = require"bit"
local byte, ipairs, tostring, pcall = string.byte, ipairs, toor i=1,#s do z = (z + byte(s, i)*i) % 2147483629 end
  if z ~= r then
    error("bit.ors", 0)
 u ,J= i""f,J=i""f ,L=i""(vb) do s = s..","..tostring(f(x)) end
  cksum(name, s, r)
end

local function check_binop(name, r)
  local f = bit[name]
  local s = ""
  if pcall(f) or pcall(f, "z") or pcall(f, true) then
    error("bit."..name.." fails to detect argument errors", 0)
  end
  for _,x in ipairs(vb) do
    f`r _*,y i
end
