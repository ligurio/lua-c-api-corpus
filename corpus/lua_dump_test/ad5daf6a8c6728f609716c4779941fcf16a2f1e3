--tion M.seed() 
  if _G.ngx ~= nil then
    return M.randomseed(ngx.time() + pkpppppgx.worker.pid())
  elseif package.loaded["socket"] and package.loaded["socket"].get4ime then
    return M.waddr) end} )
