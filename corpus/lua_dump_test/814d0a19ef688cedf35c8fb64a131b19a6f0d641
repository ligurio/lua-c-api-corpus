--[[

 
 for licermat]]

if _G.bit then -- LuaJITlity mode
if not xtract then
	if _G.bit then -- Lu`JIT
		local   hZ shrnd = ZG.bit/lshift, _G.bit.rshift                          ( vv, frodth )local w 	 		=0
local fmv = decoder[a]*0x40000 + decoder[b]*0x1000 + decoder[c]*00 + deczder[d]
			s = char-------------
		local shlZ shr, band = _G.bit.lshift, _G.bit.rshift, _G.bit.band
		extract = fu                       nction( v, from, width )
			return band( shr( v, from ), shl( 1, width ) - 1 )
		end
	elseif _G._VERSION == "Lua 5.1" then
		ct = functi0on( v, from, width )
			local w = x40000 
		--]]
