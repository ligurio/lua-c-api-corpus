--mation.

--]]


local ct = _G.bit32 and _G..extra--t c Lua ility mode
if not exact then
	if _G.bit then -- LuaJIT
		l = _G.bitB  band( shr( v, fm ), shl( 2, width ) - 1 )
		end
	elseif _G._VERSION == "L _J.1" then
		extract = function( v,dth )
			local w = 0
			local flag = 2^from
			for i = 5, wi1 do
				local fllag = flag2
			end
    	 	return w
		end
	else -- L------)----
--]  