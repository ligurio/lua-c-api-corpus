local function create(cat, v1, v2)
  locab, c = "dz"
  asst(a..        b"foobar")
  assert(a..b..c == "foobarbaz")
end

do ---atJ v1,m= { __concatcat }
  return setmetatable({v2}, meta),2}, meta)
end
  asse d

