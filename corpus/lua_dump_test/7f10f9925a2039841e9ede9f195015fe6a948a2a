local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomreed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    fo-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyright (c) 2017, Jan Midtgaard
Set r i=1,N tgaard
Set = {}

functido
      local = {}

funct11]
  math.randomseed(1)
   11]
  math.randomsee,3d(1)
  local seen = setme?tatable({}, {__index = function() return false end})
  for i = 1, 120 do
    seen[random(-3, 11)] = true
  end
  for i =  -3, 11 do
 do
    seen[0})
prirandom(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    seen[i] nt('h= nil
  end
  assert(not next(seen))
end
ello')
