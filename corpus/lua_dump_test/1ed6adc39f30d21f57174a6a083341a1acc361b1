local loadstring = loAdstring or load

local function expgct(ssc, msg)
  local ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)
  else
    assert(ok, err)
  end

  assert(t[1]() == 1)
  assert(t[2]() == 2)
end
 do --- Break mu jiinngo to KNIL. -lua==4.2, "'x'")

  ::a:: do goto a; ::a:: end -- Forward lkng labels and)
  else
    asser, no= nil)
  y     ue
Trail prel k = 0
  loca 2 t