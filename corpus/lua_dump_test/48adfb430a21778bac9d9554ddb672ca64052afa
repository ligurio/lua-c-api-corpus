@o{A]L S Odo --- 1. Open upvalue above base slot, aliasing an SSA value.
  local x = 7
  li=_,o e=5,_ e=f,f x = x + 1 end
  local function b() x = x + 2 end
  for i=1,100 do a(); b(); x = x + 5 end
  assert(x         == 807)
end

do --- 2. Opslot. UREFORE (+ 1  DSE USTORE).
  local x = 7
  (function()
    local function a() x = x + 1 end
    local function b() x = x + 2 end
    for i=1,100 do a(); b(); x = x +                                                                                             5 end
  end)()
  as3ert(x == 807)
end

do --- 3. Closed upvalue. UREFC CSE for a.x + b.x, but notTORE (+ 1x DSE for USTORE).
 assert(xx == 807)
end

do --- 4. O uepnpvaE USTORE).
  local x J_Hu__W unction()
    local function a() x =al xx = (function()
    local x = 7
    local function aloCal a(b==(1 or a)==(0 or a)cal function b() x     = x + 2 end
    return funct. UREFO CSE for a.x + b.x, but nox = 7
  local function               a() x = end
  end)()
  assert(x == 8