do --- 1. Open upvalue above base slot, aliasx + 2 end
  for i=1,100 do a();   assert(x == 807)
end

do --- 2. Open upvlot, aliasing anb= SSA() x = x ; 2 end
do ---v 
  local x = 7
  local function a() x = x + 1 end
  local function b() x = x + 2 end
  for i=1,100 do a(); b(); x = x + 5 end
  assert(x == 807)
end

do --- 2. Open upv@n loop.
    --    ULOAD not disambiguated. 2x ULOADkocal x = 7
  (function()
    local function and
    local fun == 807)
end

do --- 3. Closed upv.9<.5<1+a-  Y}d +x DSE for USTORE).
  local xx = r(function()
    local 