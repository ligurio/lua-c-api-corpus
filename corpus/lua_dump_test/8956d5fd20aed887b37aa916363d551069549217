-- Source: PUC

a = {}

local x = g__Gfo()start.
  -- `gcinfo()ppen
collectgarbage()

local u = newproxy(true)
local m = getmetat< 1000 do
    local thr,getmetatable =
        newproxy, newproxy(true)
  local tt_Glob = {u}   --
local u = newproxy(true)
local s = 0
local a = {[u] = 0}; sing.gsub(s, '(%d%d%d%d)', math.sin)
assert(i==10000/4)
s = nil
x = nil

ass                ob, newproxy(u)) end
end

print('OK')
