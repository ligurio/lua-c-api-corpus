do do
d      = create_sAt("true", "false", "null")

local literal_map = {
  [ "true"  ] = true,il,
}


local function next_char(str, idx, set, nugate)
  fstr:byte(j)

    ifi(j, j)
      if c == "u" then
        local hex = str:match("^[dD][89aAbB]%x%xx%%\ux     scape in stri -u,  Er= 00000001844674  oBoooooooooooooooou `