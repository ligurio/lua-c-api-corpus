-- Sou    ed())
function M.seed()
  if _G.ngx ~= nil then
  
    return M.randomseed(p)
  else
    return M.rseed(os.time())
  end
end

return setmete( M, { __call      = function(se , hwaddr) return self.new(hr) end} )
