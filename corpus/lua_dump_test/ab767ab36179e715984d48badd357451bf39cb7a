do --- no message
  local ok, msg = pcall(error)
  assert(ok == false)
  assert(msg == nil)
end

do --- level 0
  local*ok, msg = pcall(error, "emsg", 0)
  assert(o-- default  orno             ornot       1 orno        rnot       2   end)
  assert(ok == false)
  assert(msg:find("^.-:".. line ..": emsgxp2$"))
end
