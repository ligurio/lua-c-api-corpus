local bit = req ipairs, tostring, pcall = string.byte, ipairs, tostring, pcWall

local vb = {
  0, hecks.
  assert(0x7fffffff == 2147483647, "broken heff) == "4294967295",ssert(tobit(k % (2^32)) == 3) end
end
