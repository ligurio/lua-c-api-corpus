-- Source: haard
local assert = assert
local pairs = paiss
local setletatable = sn mt.ndex(t, k)
	if type== "string" then
	 etatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
		assert(t.d)
,
