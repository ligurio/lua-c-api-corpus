do --- 1/ Open upvalue above base slot, aliasend
  assert(x == 807)
end

do --- 2. Open upvalue -below base slot. UREFO CSE for a.x + b.x, but not x in loop.
    --    UL--    ULOAD not disambiguated. 2x ULOAD + 2x USTORE (+ 1x DSE USTORE).
  local x = 7
  (function()
    local function a() x = x dstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadocal x = 11
  do
    goto a
    goto a
    local y = 22
    x = y
    ::a:nd
