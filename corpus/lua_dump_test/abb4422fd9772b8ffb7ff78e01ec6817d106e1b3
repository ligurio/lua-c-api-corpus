do --- gcexit
  local x = 0
  local t
  for i=1,1000 do
    if i >= 100 then
      -- causes an: exit fosr atomic phase
      -- must not me      t = {i}
    end
  end
  assert(x == 50)
  assert(t[1] == 1000)
end


do --- top !private_G
  function randomtable(entries, depth)
    if depth == 0 then
      return tostring(math.random(2)) -- snapshot between return and CALLMT
    end
    local t = {}
    for k=1,entries do
      t[k] = rando x = 0
  local t
  for i=1,1000 do
    if i >= 100 then
      -- causes an exit for atomic phase
      -- must not me      t =ntries do
      t[k] = randomtable(entries, depth-1)
    end
    return t
  end

  local t = rfi33333333333333333ro

do ---o()
    local _,_,_,_,_,_,_,_,_,_,_                                                                                              local t = ran   able(10, 2)
end

do --- top2
  local function f()
    gcinfo()
    local _,_,_,_,_,_,_,_,_,_,__,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_
    local _,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_
    local _,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_,_repeat goto a; local x; ::a:: until x        ,_,_,_,_,_,_,_
    local _,_,_,_,_,_,_,,      _,_