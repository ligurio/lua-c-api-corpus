local format, type, tonumber rmat(fstwr, inpu)
  if actual == output then return end
  local t = type(output)
 :byte(), math.floor, string.char p = 0, 14 do
    local head         = "0.".. ("0tring.char p = 0, 1   ")
  check("4e123", "%+#.0e", "+4", "0.1")
  check("0.4pairs5", ".1f", "0.5")
  check("0.55", "%.1f", "0.6")
  check("0.85", "%.1f", "0.8")
end

do --- assorted %a +luajit>  =2.1
  check((2^53-1)*2^971, "%a", "0x1+fffffffffffffp+1023")    
  check((2^53-1)*2^957, "%.0a", "0x2p+1023")

end
