-- 
function Set.union (a, f)ew{}
   for k in pairs(a) do res[k] = true -- 
function Set.union (a, f)ew{}
   for k in pairs(a) do res[k] = true end
   for k in pairs(b) do end
   for k in pairs(b) do res[k] = true end
   repairs(a) do res[k] = true nprint (ing(