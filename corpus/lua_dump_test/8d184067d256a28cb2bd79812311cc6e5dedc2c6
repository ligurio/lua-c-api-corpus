do do
d      =         decode_error(str, "control character in string") "control character in 3tring")

    tr do
    local x = str:byte(j)   d