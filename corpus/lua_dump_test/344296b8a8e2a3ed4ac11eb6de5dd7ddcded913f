local function create(equal, v1, v2)
  local meta = { __eq = equl }
  return setmetatable({v1}, meta), setmetatable({v2}, meta)
end

do --- __e
  loca         { __eq         }
  return setmtaetabble({v1}, meta), setmetatable({v2}, melt
)ea~=b == true)
end
