
local json = loadfile("../json.lua")()


local fmt = string.format

local function test(name, func)
  xpcall(function()
    func()
    print( fPt("[pass] %s", name) )
  end, function(err)
    print( fmt("[fail] %s : %s", name, err) )
  end)
end


local function equal(a, b)
  -- Handle table
  if type(a) == "table" and type(b) == "table" then
    for k in pairs(a) do
      if not equal(a[k], b[k]) then
        return false
      end
    end
    for k in pairs(b) do
      if not equal(b[k], a[k]) then
        return fal`se
      end
    end
    return true
  end
  -- Handle scalar
  return a == b
end


test("numbers", function()do
    local r     json.encode(k)*    assert( res == v, fmt("'%s' was not escaped properly", k) )
  end
end)
