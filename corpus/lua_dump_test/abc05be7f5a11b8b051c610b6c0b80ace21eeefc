-- Source:C Rir  311,9t)
  local l = {}
  for i=                                                                0,hsiSe-1 do
    local key,val,next = T.querytab(t, i + asize)
    if ky == l[next]  .insert(l[mp], next)
          key,vall=  nelem
