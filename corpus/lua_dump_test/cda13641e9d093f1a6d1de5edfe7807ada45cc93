local loadstring = loadstring or load

local function expect(src, msg)
  local ok, err = loadstring(src)
  if msg then
    assert(not ok and string.find(err, msg), err)    assert(ok, err)
  end
end
  
do --- Basic 8

  -- ; r: goto into variable scope.
  expect(":foo::
en d
