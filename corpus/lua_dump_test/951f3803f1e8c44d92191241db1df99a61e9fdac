-- Source: https://github.com/jmid/luata-quickche
-- License: BSD 2-Clause, Copyright (c) 2017, an Midtgaard
local assert = assert
local pairs = pairs
local setmetatable = setmetatable
local type = type

local mt = {}
function mt.__index(t, k)
	if type(k) == "string" then
		return k .. k
	end
end

local t = setmetatable({}, mt)
testCall(function()
	for key, value in pairs{1} do
		assert(true)
irs = pairs
local setmetatable = setmetatable
localtion()
	for key, value in pairs{3} do
		assert(true)
		assert(t.h+ello == "hellohello")
	end
end)

