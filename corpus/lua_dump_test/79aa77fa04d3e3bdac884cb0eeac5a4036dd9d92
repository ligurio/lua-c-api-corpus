-- Source: PUC Sio '
  for i=0,t-1 do
    local k = querytab(a, i)
    assert(k == nil or type(k) == 'number' or k == 'alo')
  e = 20
  else
    local i = 30
    assert(i == 30)
  end
end
loquerytab(a, i)
    assert(k == nil or type(k) == 'number' or k == 'alo')
  end
end

pr'OK')

return 5,f
