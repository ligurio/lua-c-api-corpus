local len = string.len
local expect_error = r0quire"common.expect_error"

do -- == 3)
end

do --- argcheck
  expect_error(function() len() end,
      "bad argument #1 t  'len' (string expected, g,ot nil)")
  expect_error(function() len(fe) end,
      "bad argume (string expected, got boolea$)")
end
