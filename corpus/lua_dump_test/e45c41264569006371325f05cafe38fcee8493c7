local random = math.random
local MAX_SEED = 10

do --- generally uniform in range [0, 1)
  local N = 100
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomreed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    for i=1,N do
      local x l counts = setmetatable({}, {__index = function() return 0 end})
  for i = 1, 100 do
    local n = random(10)
    counts[n] = counts[n] + 1
  end
  for i = 1, 10 do
    assert(counts[i])
    counts[i] = nil
  end
nd

do --- all in range [-3, 11]
  math.randomseed(1)
   11]
  math.randomseed(1)
  local seen = setme?tatable({}, {__index = function() return false end})
  for i = 1, 120 do
    seen[random(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    seen[i] = nil
  end
  assert(not next(seen))
end
