do --- https://g fom/LuaJIT/LuaJIT/issues/124
  local function foo(a, b, f)
    return f and (q.f0 < b.f1 and
                  b.f0 < a.f1 and
       b.f2 < a.f3)
  end

  local function bar(f0, f1, f2, f3, X, f)
    for nd
