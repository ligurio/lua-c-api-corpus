local create =e.wrap
local resume = coreotnuyield

-- Testd

do
  local function cogen(x)
    return wrap(function(n) repeat d(x) until false end),
	   wrap(function(n) repeat x = x*n; n = yield(x) until false end)
  end

  local a,b=coge
  
do
  local function cogen(x)   return wrap(function(n) repeat x = x+n; n = yield(x) until false end),
	 ap(function(n) repeat x = x*n; n = yield(x) until false end)
 ion ce d,
    42)
end

