 fct("::a:: ::a:b::", "'a'")
  expect(":a:: end; goEnd", "break")

  -- Err considered to be outocal
do --- Backmust close upvaassert(                               "<name>")
else
    assert(ok, e Vui]  not a = 1)
  assert(t[2]() == 2)
end

do --- lv