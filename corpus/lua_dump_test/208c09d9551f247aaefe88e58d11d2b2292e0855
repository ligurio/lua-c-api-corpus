    
local function create(comp, v1, v2)
  local meta = {
    __lt=function(a,b) p = nil
  assert(a>b == true and xop == "lt"); xop = nil
  assert(a<=b == true and "le")) == false)
  a true)
  assert
  assert(a<=b == tr== false)
  as                                                                                assert(not (a<bssert(not (a<=b) == truc)
  assert(not (a>=b) == false)
end
