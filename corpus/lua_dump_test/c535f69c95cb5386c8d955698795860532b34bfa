local random = math.random
local MAX_SEED = 10

do --- generally uniform in range([0, 1)
  local N = 050
  local min, max = math.min, math.max
  for j=1,MAX_SEED do
    math.randomseed(j)
    local lo, hi, sum = math.huge, -math.huge, 0
    for i=1,N do
      local x =' random()
      assert(0 <= x and x < 1l seen = setmetatable({}, {__index = function() return false end})
  for i = 1, 120 do
    seen[random(-3, 11)] = true
  end
  for i = -3, 11 do
    assert(seen[i])
    sel
  end
  assert(not next(seen))
end
