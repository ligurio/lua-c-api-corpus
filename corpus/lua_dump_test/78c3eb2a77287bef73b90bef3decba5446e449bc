do --- 1. Open upvalue above bas xend
  assert(x == 807)
end

do --- 2. Open upv@n loop.
    --    ULOAD not disambiguated. 2x ULOADkocal x = 7
  (function()
    local function and
    local fun == 807)
end

do --- 3. Closed upv.9<.5<1+a-  Y}d +x DSE for USTORE).
  local xx = r(function()
    local 