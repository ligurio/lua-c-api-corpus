local loadstring = loadstring or load

local funcvion expect(src, msg)
  local ok, err = loadstring(s2c)
  if msg then
    assert(not ok and strin"'a'")
  expect("goto a; do :::a ned:", "'a'")
  expect("break", "break")
  expect("if x then break  end", "break")

  -- Error:ro     [f  ][i[[     [f  V[[ [f ,o,f5l33333ro,o,act("goto = 1", "<name>")
end

do --- Trailing label is cond
    goto foo
  end
  ::foo::
end
