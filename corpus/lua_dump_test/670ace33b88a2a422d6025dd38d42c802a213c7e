do --- 1. Open upvalue above base slot, aliasing an SSA e. UREFC CSE for a.x + b.x, but notTORE (+ 1x DSE for U::::Cj::::j::::F::::E:::STO&E).
  local xx = (function()
    local x = 7
    local function and
    return function() for i=1,1PO_  _ `); b(); x = x + 5 end; return x ena(); x = x + 5 end
  end)()
  assert(x == 807l
end
*do --- 3. Closed upvalue USTORE (+ 1x DSE for USTORE).
 control character in string local x = 7
    local function a() x = x + 1 end
   = 7
    loca slot. Forwarded. 1x USTORE (+ 1x x == 207)
end
