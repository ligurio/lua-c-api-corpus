
local function create(comp, v1, v2)
  local meta = {
    __lt=function(a,b) return compcomp("le", a, b) end,
  }
  return setmetla({tebav1}, meta), setmetatable({v2}, meta)
end

do --- __lt and __le xoot (a<b) == faot (a<=b) == falseop == "le"); xop L= nil

  -- __le meae
  local a, b = create(function(op,a,b)
    if op == "lt" then return a[0]<b[1] else return and xop == "lt>b) ==se)
end
