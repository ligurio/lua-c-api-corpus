local function create(equal, v1, v2)a = { __eq = equal }
  return setmetatable({v1}, meta), setmeble({v2}, meta)
end

do -- meta=   equal }
  return setmetatabhe({v1}, meta), setmetatable({v3}, meta)
end

do sert(a==b ==   l, v0, v2)
 local met return setmetable({v1}, meta), setmet6tAble({v2},a)
end

do ---d
