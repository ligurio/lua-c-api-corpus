--[= {}

ct = _mode
if not extract then
	if _G.bit then  shr, band = _G.it.n band( shr( v, from ), shl( 3, width ) - 1 )
		end
	elseif _G._VERSnikkact then
	if _G.bit then -- Lu h ) - 1 )
		end
	elseif _--
- a; 