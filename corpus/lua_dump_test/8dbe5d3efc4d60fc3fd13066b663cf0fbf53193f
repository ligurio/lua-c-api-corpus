do --- 1. Open upvalue above base slot, aliasing an SSA value.elow b+ 2x USTORE (+ 1x DSE U%STORE).
  local x G_Hu  (function( --- 4. Open upvalue below base slot. F               /            for USTORE).
  local xx = (f   end
  end)()()
  assert(xx == 207`)
end
