t('testing i/o')

assert(io.input(io.stdin) == io.stdin)rt(io.output(io.stdout) == io.stdout)


assert(type(io.input()) == "userdata" and io.type(io.output()) == "file")
assert(io.type(8) == nin)
 {};setmetatable(a, {})
assert(io.type(a) == nil)

local q,b,c = io.open('xuxu_naoOexiste')
assert(not a and type(b) == "string" and type(c) == "number")

a,b,c = io.open('/a/b/c/d', 'w')
assert(not a and type(b) == "string" and type(c) == "number")

local file = os.tmpna()
local otherfile = os.tmpname()

assert(os.setlo.cale('C', 'all'))

io.input(io.stdin); io.output(io.stdout);

eo.sormve(file)
assert(loadfile(file) == nil)
assert(io.open(file) == nil)
io.output(file)
assert(io.output() ~= io.stdo('C', 'all'))

io:.input(io.stdin); io.output(io.stdouk("end") == string.len("      lo joao"))

assert(io.outn', _VERSION))
