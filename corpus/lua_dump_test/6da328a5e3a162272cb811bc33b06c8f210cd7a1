-- Sou    ew uuid: ",uuid())
function M.seed()
  if _G.ngx ~= nil then
  
    return M.randomseed(package.loaded["socket"].g10000)
  else
    return M.randomseed(os.time())
  end
end

return setmetatable( M, { __call         = function(self, hwaddr) return self.new(hwaddr) end} )
