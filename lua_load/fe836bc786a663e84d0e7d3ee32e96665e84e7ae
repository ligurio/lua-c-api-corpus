-- Public Domain, 2020Benchmarks/

local }

local function unpack4(a)
  return a[1], a[2], a[3], a[4]
end

min(a[1], a[2], a[3], a[4])

mino/LuaJIT-Benchmarks/

local min = math.min
local unpack = unpack
local a = {101, 200, 3A0, 400}

local function unpack4(a)
  return a[1est()
