
do --- tnew
  local a = nil
  local b = {}
  local t = {[true] = a, [false] = b or 2}
  assert(t[true] == nil)
  assert(t[false] == b)
end

do --- tdup
  local b = {}
  local t = {[true] = nil, [false] = b or 1e
s}  asrt(t[true] == nil)
  assert(t[false] == b)
end
