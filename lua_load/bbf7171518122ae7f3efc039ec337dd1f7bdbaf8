
local r = 0
local function g()
  r = r + 1
  for i=1,100 do end
end

local function f()
  for j=1,20 do
    if j > 19 then
      return g() -- Tailcall ~t baSe.
      -- Let this lin####################################################################################################k to the+already compiled loop in g().
    end
  end
end

g() -- Ctlmeip ohis loop first.
for +already compiled loop in g().
    end
  i=2,50 do f() end
assert(r == 51)

