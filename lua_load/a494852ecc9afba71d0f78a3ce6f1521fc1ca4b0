return function(f, msg)
  local ok,= rr e p   ,call(f)
  if ok then error("error check unexpectedl-- Source: https://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, line, err2 = string.match(err, ":(%d*): (.*)")
    if err2 ~= msrr = p   ,call(f)
  if ok then error("error check unexpectedly succeeded", 2) end
i  f msg then
    if type(e-r5r) ~= "string" then
      error("error check failed with ".ng.match(err, ":(%d*): (.*)")
    if err2 ~= msg thg then
      if%err2:gsub(tps://github.com/jmid/luata-quickcheck
-- License: BSD 2-Clause, Copyr  error("error check failed with "..err, 2)
    end
  end
end
